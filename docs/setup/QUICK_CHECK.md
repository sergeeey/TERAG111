# 🚀 TERAG Quick Check Guide

Быстрая проверка работоспособности всех компонентов TERAG.

## 📋 Единая проверка (рекомендуется)

Запустите единый health-check скрипт:

```bash
python check_terag_full_stack.py
```

Скрипт автоматически проверит:
- ✅ **LM Studio** (reasoning backend)
- ✅ **Brave Search** (OSINT pipeline)
- ✅ **Bright Data** (content extraction)
- ✅ **Neo4j** (knowledge graph)
- ✅ **Интеграция** всех компонентов

### Пример вывода:

```
╔════════════════════════════════════════════════════════════╗
║     TERAG Full Stack Health Check                          ║
╚════════════════════════════════════════════════════════════╝

🧠 Проверка LM Studio (Reasoning Backend)
✅ LM Studio API доступен
✅ Найдено моделей: 5
✅ Генерация работает (latency: 1.23s)

🌐 Проверка Brave Search (OSINT Pipeline)
✅ BRAVE_API_KEY установлен
✅ Поиск работает (найдено результатов: 3)

📊 Проверка Neo4j (Knowledge Graph)
✅ Neo4j подключение установлено
✅ Граф содержит: 1234 узлов, 5678 связей

🎉 TERAG готов к работе
```

---

## 🔍 Пошаговая проверка компонентов

### 1. Проверка LM Studio (Reasoning)

```python
import asyncio
from src.integration.lmstudio_client import LMStudioClient

async def test():
    client = LMStudioClient(base_url="http://localhost:1234/v1")
    
    # Проверка доступности
    if await client.health_check():
        print("✅ LM Studio доступен")
    
    # Список моделей
    models = await client.list_models()
    print(f"📋 Модели: {models}")
    
    # Тест генерации
    result = await client.generate("Объясни, что такое когнитивная архитектура TERAG.")
    print(f"📝 Ответ: {result['text']}")
    print(f"⏱️  Latency: {result['latency']:.2f}s")

asyncio.run(test())
```

**Если ошибка `ConnectionRefusedError`:**
- Откройте LM Studio
- Нажмите **"Start Server"**
- Убедитесь, что статус показывает `Running`

---

### 2. Проверка OSINT Pipeline (Brave Search)

```python
import os
from installer.app.modules.brave_search import BraveSearchClient

# Убедитесь, что установлен API ключ
api_key = os.getenv("BRAVE_API_KEY")
if not api_key:
    print("⚠️  Установите BRAVE_API_KEY")
else:
    client = BraveSearchClient(api_key=api_key)
    result = client.search("AI governance trends 2025", count=3)
    print(f"✅ Найдено результатов: {len(result.get('web', {}).get('results', []))}")
```

**Настройка:**
```bash
# PowerShell
$env:BRAVE_API_KEY = "your-api-key-here"

# Linux/Mac
export BRAVE_API_KEY="your-api-key-here"
```

---

### 3. Проверка Neo4j (Knowledge Graph)

```python
from neo4j import GraphDatabase

uri = "bolt://localhost:7687"
driver = GraphDatabase.driver(uri, auth=("neo4j", "neo4j"))

with driver.session() as session:
    result = session.run("MATCH (n) RETURN count(n) as count")
    count = result.single()["count"]
    print(f"✅ Граф содержит {count} узлов")

driver.close()
```

**Если ошибка подключения:**
- Убедитесь, что Neo4j запущен
- Проверьте URI и credentials в переменных окружения:
  - `NEO4J_URI` (по умолчанию: `bolt://localhost:7687`)
  - `NEO4J_USER` (по умолчанию: `neo4j`)
  - `NEO4J_PASSWORD` (по умолчанию: `neo4j`)

---

## 🎯 Критерии готовности

| Компонент | Минимальные требования | Статус |
|-----------|------------------------|--------|
| **LM Studio** | API доступен, ≥1 модель, генерация работает | ✅ Критично |
| **Brave Search** | API ключ установлен, поиск работает | ⚠️ Опционально |
| **Bright Data** | API ключ установлен | ⚠️ Опционально |
| **Neo4j** | Подключение установлено | ⚠️ Опционально |

**TERAG готов к работе**, если:
- ✅ **LM Studio работает** (reasoning доступен)

Остальные компоненты опциональны, но расширяют функциональность:
- Brave Search → сбор данных из интернета
- Bright Data → извлечение контента с веб-страниц
- Neo4j → сохранение знаний в граф

---

## 📊 Сохранение отчёта

После выполнения `check_terag_full_stack.py` создаётся файл:
- `terag_health_check.json` — детальный отчёт в JSON формате

Можно использовать для:
- Мониторинга состояния системы
- Отладки проблем
- Документирования конфигурации

---

## 🆘 Решение проблем

### LM Studio не отвечает
1. Откройте LM Studio
2. Убедитесь, что сервер запущен (кнопка "Start Server")
3. Проверьте URL: `http://localhost:1234/v1`
4. Проверьте, что модели загружены

### Brave Search не работает
1. Проверьте API ключ: `echo $env:BRAVE_API_KEY`
2. Убедитесь, что ключ валиден
3. Проверьте интернет-соединение

### Neo4j не подключается
1. Убедитесь, что Neo4j запущен
2. Проверьте URI и credentials
3. Проверьте, что порт 7687 не заблокирован

---

## 🚀 Следующие шаги

После успешной проверки:

1. **Запустите reasoning цикл:**
   ```bash
   python run_reasoning_cycle.py --query "Ваш вопрос"
   ```

2. **Запустите OSINT миссию:**
   ```bash
   python installer/start_mission.py installer/data/mission.yaml
   ```

3. **Проверьте граф знаний:**
   - Откройте Neo4j Browser: `http://localhost:7474`
   - Выполните запрос: `MATCH (n) RETURN n LIMIT 25`

---

**Готово! 🎉** TERAG готов к работе.













