# ✅ Исправление конфликта портов TERAG MCP

**Дата:** 2025-01-27  
**Проблема:** Конфликт портов между mcp.json и terag_mcp_server.py

---

## 🔍 Диагностика

### Проверка контейнера:
```bash
docker ps --format "table {{.Names}}\t{{.Ports}}" | findstr terag
```

**Результат:**
```
terag-api        0.0.0.0:8001->8001/tcp
```

### Проверка API:
```bash
curl http://localhost:8001/api/health
```

**Результат:**
```json
{"status":"ok","timestamp":"2026-01-06T05:45:46.973360+00:00"}
```

**Вывод:** Контейнер работает на порту **8001**, API доступен на **8001**.

---

## ✅ Исправления

### 1. Обновлен `mcp.json`:
```json
"TERAG_API_URL": "http://localhost:8001"  // ✅ Исправлено с 8000 на 8001
```

### 2. Обновлен `terag_mcp_server.py`:
```python
TERAG_API_URL = os.getenv("TERAG_API_URL", "http://localhost:8001")  // ✅ Исправлено
```

---

## 📝 Важное замечание

MCP сервер запускается **внутри контейнера** через `docker exec`, поэтому:
- Он обращается к API по `localhost:8001` (внутри контейнера)
- Это тот же контейнер, где работает API
- Порт должен совпадать с портом, на котором слушает API внутри контейнера

---

## ✅ Статус

- ✅ Порт в `mcp.json`: **8001** (исправлено)
- ✅ Порт по умолчанию в `terag_mcp_server.py`: **8001** (исправлено)
- ✅ Контейнер работает на **8001**
- ✅ API доступен на **8001**

**Конфликт портов устранен!** 🎉

---

**Последнее обновление:** 2025-01-27
