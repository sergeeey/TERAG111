# 🚀 TERAG Deployment Pack

## 📦 Описание пакета

**TERAG Deployment Pack** — готовый к развёртыванию пакет когнитивной системы TERAG v1.0.

### ✨ Включено в пакет

- ✅ Полностью рабочая система
- ✅ AI-REPS дашборд с метриками
- ✅ Граф знаний (584 документа)
- ✅ Чат-интерфейс с историей
- ✅ Интеграция с Ollama (8 моделей)
- ✅ Мультиязычность (RU/EN)
- ✅ API для внешних приложений

### 📊 Статус системы

- **Документов обработано:** 584
- **Триплетов извлечено:** 101,117
- **Узлов в графе:** 202,234
- **Статус:** Production Ready

---

## 🚀 Быстрый старт

### 1. Проверка требований

```powershell
# Python 3.8+
python --version

# Проверка Ollama
ollama list
```

### 2. Запуск системы

```powershell
# Запуск API сервера
python scripts/run_api.py
```

### 3. Открыть дашборд

```
http://127.0.0.1:8000/api/static/index.html
```

---

## 🌐 Развёртывание для команды

### Шаг 1: Запуск на хосте

```powershell
# Запуск с доступом из сети
python scripts/run_api.py --host 0.0.0.0 --port 8000
```

### Шаг 2: Настройка брандмауэра

```powershell
# Разрешить входящие соединения на порт 8000
New-NetFirewallRule -DisplayName "TERAG API" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow
```

### Шаг 3: Доступ для команды

Узнайте IP вашего компьютера:
```powershell
ipconfig
```

Команда может открыть:
```
http://<ваш_IP>:8000/api/static/index.html
```

---

## 📊 Доступные API

### Основные эндпоинты

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/metrics` | GET | Метрики AI-REPS |
| `/api/graph/stats` | GET | Статистика графа знаний |
| `/api/query?question=<текст>` | GET | Поиск по графу знаний |
| `/api/llm_query?question=<текст>&model=<model>` | GET | Запрос к LLM |
| `/api/llm/models` | GET | Список доступных моделей |
| `/api/health` | GET | Статус системы |
| `/api/set_language` | POST | Установить язык интерфейса |
| `/api/get_language` | GET | Получить текущий язык |

### Примеры использования

```bash
# Получить метрики
curl http://127.0.0.1:8000/api/metrics

# Задать вопрос через Graph RAG
curl "http://127.0.0.1:8000/api/query?question=Что такое платежи"

# Задать вопрос через LLM
curl "http://127.0.0.1:8000/api/llm_query?question=Hello&model=mistral:latest"

# Получить список моделей
curl http://127.0.0.1:8000/api/llm/models
```

---

## 🔧 Автоматизация

### Добавление новых документов

Документы добавляются в `data/`, система автоматически конвертирует и индексирует их.

### Автозапуск при старте Windows

Создайте файл `start_terag.bat`:

```batch
@echo off
cd /d D:\TERAG111-1
python scripts/run_api.py --host 0.0.0.0
```

Добавьте в автозагрузку Windows.

---

## 🧪 Тестирование

### Проверка API

```powershell
# Проверка здоровья системы
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/health"

# Тест метрик
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/metrics"

# Тест графа знаний
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/graph/stats"
```

---

## 📈 Мониторинг

### Метрики системы

Система отслеживает:
- **RSS** (Резонанс Состояния Системы)
- **COS** (Когерентность Операционных Состояний)
- **FAITH** (Надёжность и Целостность)
- **Growth** (Рост Знаний)
- **Resonance** (Фазовый Резонанс)
- **Confidence** (Уверенность Системы)

### Логирование

Логи сохраняются в `logs/` директории.

---

## 🔐 Безопасность

### Рекомендации

1. **Локальный доступ:** Запуск по умолчанию только для `localhost`
2. **Сетевой доступ:** Используйте только в доверенной сети
3. **Брандмауэр:** Закрывайте неиспользуемые порты
4. **Документы:** Конфиденциальные документы храните в `data/`

---

## 🎯 Типичные сценарии использования

### 1. Анализ документов

```
Пользователь → TERAG: "Проанализируй все отчёты за 2024 год"
TERAG → Граф знаний: [ищет релевантные документы]
TERAG → LLM: [формирует консолидированный ответ]
LLM → Пользователь: [сводный отчёт с ссылками на источники]
```

### 2. Диалог с системой

```
Пользователь: "Что такое платежи?"
TERAG: [поиск в графе → извлечение фактов → формирование ответа → визуализация связей]
```

### 3. Мультиязычный режим

```
Пользователь переключает язык интерфейса → RU/EN
TERAG автоматически определяет язык запроса
LLM отвечает на соответствующем языке
```

---

## 📚 Дополнительная информация

- **Документация LLM интеграции:** `docs/TERAG_LLM_INTEGRATION.md`
- **Архитектура:** `docs/architecture/`
- **API справка:** `http://127.0.0.1:8000/docs` (Swagger UI)

---

## ✅ Статус: Production Ready

Система полностью готова к использованию:
- ✅ Все компоненты протестированы
- ✅ API стабилен
- ✅ Документация актуальна
- ✅ Метрики в норме
- ✅ Интеграции работают

**Версия:** 1.0
**Дата:** 2025-10-26
**Статус:** ✅ Production Ready



























