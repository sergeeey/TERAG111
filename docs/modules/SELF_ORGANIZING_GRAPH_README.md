# 🌱 Self-Organizing Knowledge Graph — Живой граф смыслов

## 🎯 Концепция

**Self-Organizing Graph** — это эволюция TERAG Graph от жёсткой схемы данных к **живому, саморазвивающемуся графу смыслов**, который сам создаёт новые блоки, связи и категории при встрече с новым знанием.

### Принципы:

* **Сходство → объединение** — похожие концепты объединяются
* **Несходство → создание нового** — новые концепты создают новые кластеры
* **Повторение → усиление** — часто встречающиеся связи усиливаются
* **Противоречие → ветвление** — противоречивые факты создают альтернативные ветви

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install sentence-transformers numpy
```

### 2. Инициализация

```python
from src.core.self_organizing_graph import SelfOrganizingGraph
from src.integration.lmstudio_client import LMStudioClient
from installer.app.modules.graph_updater import GraphUpdater
from sentence_transformers import SentenceTransformer

# Инициализация компонентов
lm_client = LMStudioClient()
await lm_client.connect()

graph_updater = GraphUpdater()
embedder = SentenceTransformer('all-MiniLM-L6-v2')

sog = SelfOrganizingGraph(
    graph_updater=graph_updater,
    lm_client=lm_client,
    embedder=embedder,
    similarity_threshold=0.65  # Порог для создания нового кластера
)
```

### 3. Категоризация и сохранение

```python
# Новый факт
text = "В психологии код-ревью усиливает эмпатию между разработчиками"

# Динамическая категоризация
result = await sog.categorize_and_store(
    text=text,
    source_url="https://example.com/article",
    confidence=0.8
)

if result['action'] == 'create_new_cluster':
    print(f"✅ Создан новый кластер: {result['cluster_name']}")
else:
    print(f"✅ Связан с существующим: {result['cluster_name']}")
```

---

## 📚 API Reference

### `SelfOrganizingGraph.__init__(graph_updater, lm_client=None, embedder=None, similarity_threshold=0.65, decay_days=30)`

Инициализация Self-Organizing Graph.

**Параметры:**
- `graph_updater`: Экземпляр `GraphUpdater`
- `lm_client`: Экземпляр `LMStudioClient` (опционально)
- `embedder`: Экземпляр `SentenceTransformer` (опционально)
- `similarity_threshold`: Порог схожести для создания нового кластера (0.0-1.0)
- `decay_days`: Количество дней до начала decay неиспользуемых концептов

---

### `async categorize_and_store(text, source_url=None, confidence=0.8) -> Dict`

Категоризировать и сохранить текст в граф.

**Параметры:**
- `text` (str): Текст для категоризации
- `source_url` (str, optional): URL источника
- `confidence` (float): Уровень уверенности (0.0-1.0)

**Возвращает:**
- `Dict`: Информация о действии:
  - `action` (str): "create_new_cluster" или "link_to_cluster"
  - `cluster_name` (str): Название кластера
  - `cluster_type` (str, optional): Тип кластера ("Emergent")
  - `similarity` (float): Схожесть с ближайшим кластером

**Пример:**
```python
result = await sog.categorize_and_store(
    text="Квантовые вычисления для машинного обучения",
    source_url="https://example.com",
    confidence=0.8
)
```

---

### `async find_similar_clusters(embedding, limit=5) -> List[Tuple[str, float]]`

Найти похожие кластеры по embedding.

**Параметры:**
- `embedding` (np.ndarray): Embedding вектор
- `limit` (int): Максимальное количество результатов

**Возвращает:**
- `List[Tuple[str, float]]`: Список кортежей (cluster_name, similarity)

**Пример:**
```python
embedding = sog._get_embedding("Психология программирования")
similar = await sog.find_similar_clusters(embedding, limit=3)
```

---

### `async should_create_new_cluster(text, embedding=None) -> Tuple[bool, Optional[str], float]`

Решить, нужно ли создавать новый кластер.

**Параметры:**
- `text` (str): Текст для анализа
- `embedding` (np.ndarray, optional): Embedding текста (если уже вычислен)

**Возвращает:**
- `Tuple[bool, Optional[str], float]`: (should_create, best_cluster_name, max_similarity)

---

### `async create_emergent_concept(name, description, confidence=0.75, source_url=None) -> Dict`

Создать emergent концепт (новый кластер).

**Параметры:**
- `name` (str): Название концепта
- `description` (str): Описание концепта
- `confidence` (float): Уровень уверенности
- `source_url` (str, optional): URL источника

**Возвращает:**
- `Dict`: Информация о созданном концепте

---

### `apply_growth_rules()`

Применить правила роста графа.

**Правила:**
- Если два узла часто встречаются вместе → добавить связь `:RELATED_TO`
- Если ветка быстро растёт → повысить `priority`

**Пример:**
```python
sog.apply_growth_rules()
```

---

### `decay_old_concepts()`

Уменьшить вес неиспользуемых концептов.

**Правило:**
- Если концепт не обновлялся > `decay_days` дней → уменьшить `decay_score`

**Пример:**
```python
sog.decay_old_concepts()
```

---

## 🔄 Цикл самоорганизации

```
1. Новый факт → Векторизация → Сравнение с кластерами
2. Если схожесть < 0.65 → LM Studio решает: создать новый?
3. Если да → Создаётся Emergent концепт
4. Если нет → Связывается с ближайшим кластером
5. Правила роста применяются периодически → Граф эволюционирует
```

---

## 🧪 Тестирование

### Запуск теста

```bash
python scripts/tests/test_self_organizing_graph.py
```

Тест проверяет:
- ✅ Категоризацию нового текста
- ✅ Поиск похожих кластеров
- ✅ Применение правил роста
- ✅ Decay старых концептов

---

## 📊 Примеры использования

### Пример 1: Создание нового кластера

```python
# Текст, который не похож ни на что существующее
text = "В психологии код-ревью усиливает эмпатию"

result = await sog.categorize_and_store(text)

# Результат: создан новый кластер "Cognitive Collaboration" (Emergent)
```

### Пример 2: Связывание с существующим

```python
# Текст, похожий на существующий кластер
text = "Python error handling best practices"

result = await sog.categorize_and_store(text)

# Результат: связан с кластером "Programming" (similarity: 0.78)
```

### Пример 3: Применение правил роста

```python
# Периодически применяем правила роста
sog.apply_growth_rules()

# Граф автоматически создаёт новые связи между часто встречающимися концептами
```

---

## 🔗 Интеграция

### С Learning Bridge

```python
from src.integration.learning_bridge import LearningBridge

# Используем Self-Organizing Graph вместо прямого GraphUpdater
bridge = LearningBridge(
    lm_client=lm_client,
    graph_updater=sog  # Используем SOG вместо обычного GraphUpdater
)

# Reasoning с контекстом будет использовать самоорганизующийся граф
result = await bridge.reason_with_context(
    question="What are best practices?",
    domain=None,  # SOG сам определит домен
    save_result=True
)
```

---

## 🧠 Подсказки

* **Порог схожести**: 0.65 — настраиваемый параметр (можно экспериментировать)
* **Embeddings**: Используйте `all-MiniLM-L6-v2` для баланса скорости и качества
* **Кэширование**: Кластеры кэшируются для производительности
* **Graceful degradation**: SOG работает даже если embeddings недоступны (fallback)
* **Правила роста**: Применяйте периодически (например, раз в день)

---

## 📈 Ожидаемый результат

После использования Self-Organizing Graph:

* ✅ TERAG сам создаёт новые блоки знаний при встрече с новым
* ✅ Граф эволюционирует без жёстких ограничений
* ✅ Система объединяет похожие концепты автоматически
* ✅ Появляются emergent концепты — живые ветви графа
* ✅ Граф самобалансируется через правила роста

---

## 🌱 Эволюция графа

### Этапы:

1. **Начало**: Граф пуст, все концепты создаются как Emergent
2. **Рост**: Появляются кластеры, связи между ними усиливаются
3. **Объединение**: Похожие кластеры объединяются
4. **Ветвление**: Противоречивые факты создают альтернативные ветви
5. **Балансировка**: Старые концепты теряют вес, новые усиливаются

---

**Готово!** TERAG теперь может строить свою карту мира динамически! 🎯✨













