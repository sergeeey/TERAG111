# 🔍 OSINT Digest — Ежедневный аналитический бриф

## 📋 Описание

OSINT Digest автоматически добавляется в ежедневный Cognitive Ops Report и содержит:
- **Топ-3 новых сигнала** за последние 7 дней
- **Анализ трендов** на основе обнаруженных сигналов
- **Статистику** (средняя новизна, уверенность, количество сигналов)
- **Топ источники** (домены, откуда пришли сигналы)

## 🔧 Как это работает

### Источники данных

Digest получает сигналы из двух источников (в порядке приоритета):

1. **Neo4j Graph** — если граф доступен, берёт сигналы из узлов типа `Signal`
2. **Файлы** — читает из `data/weak_signals.json` и других файлов миссий

### Генерация дайджеста

1. Собирает последние сигналы за 7 дней
2. Сортирует по `novelty_index` и `confidence`
3. Берёт топ-3
4. Анализирует тренды и статистику
5. Форматирует для Telegram (Markdown)

## 📊 Формат дайджеста

```
🔍 OSINT Digest

Топ-3 новых сигнала:

1. 🔥 Signal Name
   Description...
   новизна: 0.85, уверенность: 0.78
   [Источник](https://...)

2. ⭐ Another Signal
   ...

Тренды:
• Высокая новизна сигналов
• Высокая уверенность

Статистика: Средняя новизна: 0.75, Средняя уверенность: 0.78

Топ источники: arxiv.org, example.com
```

## 🧪 Тестирование

Проверьте генерацию дайджеста:

```bash
python scripts/tests/test_osint_digest.py
```

## 🔄 Интеграция

Digest автоматически добавляется в ежедневный отчёт через `telegram_service.py`:

```python
from src.integration.osint_digest import generate_daily_digest

digest = generate_daily_digest()
# Добавляется в ежедневный отчёт
```

## 📝 Настройка

### Источники сигналов

Сигналы могут приходить из:
- **Neo4j** — после выполнения миссий с `graph_updater`
- **Файлы** — `data/weak_signals.json`, результаты миссий

### Параметры

В `osint_digest.py` можно настроить:
- `days` — период для поиска сигналов (по умолчанию 7 дней)
- `limit` — количество сигналов для анализа (по умолчанию 3)

## 🚀 Будущие улучшения

- [ ] Интеграция с LM Studio для генерации кратких выводов
- [ ] Визуализация трендов (графики)
- [ ] Категоризация сигналов по доменам
- [ ] Сравнение с предыдущими периодами

---

**Готово!** Ежедневный отчёт теперь включает аналитический бриф! 🎯✨













