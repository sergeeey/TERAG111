# 🧠 TERAG Evolution Loop — Документация

## 📋 Обзор

**TERAG Evolution Loop** — главный оркестратор цикла рассуждения, объединяющий:

- **KAG-Solver** — графовое рассуждение на основе Knowledge-Augmented Graph
- **Multi-Agent System** — система агентов для планирования, исследования, проверки, написания и критики
- **Auto-Eval 2.0** — автоматическая самооценка системы
- **Ethical Core** — этическая фильтрация контента
- **Visualization** — визуализация reasoning путей

---

## 🏗️ Архитектура

```
TERAG Evolution Loop
├── Planner Agent      → Планирование рассуждения
├── Researcher Agent   → Сбор данных из графа
├── KAG-Solver        → Графовое рассуждение
├── Verifier Agent    → Проверка достоверности
├── Ethical Filter    → Этическая фильтрация
├── Writer Agent      → Составление отчёта
├── Critic Agent      → Критический анализ
├── Auto-Evaluator    → Обновление метрик
└── Graph Dashboard   → Визуализация
```

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install networkx pyvis
```

### Базовое использование

```python
from src.core.evolution_loop import TERAGEvolutionLoop
import asyncio

async def main():
    # Инициализация
    loop = TERAGEvolutionLoop(
        graph_driver=neo4j_driver,
        lm_client=lm_studio_client,
        enable_visualization=True
    )
    
    # Запуск цикла
    result = await loop.run("Как TERAG может оптимизировать OSINT анализ?")
    
    # Результат
    print(result["final_report"]["conclusion"])

asyncio.run(main())
```

---

## 📚 Компоненты

### 1. KAG-Solver

Графовое рассуждение на основе Knowledge-Augmented Graph.

**Функции:**
- Извлечение концептов из запроса
- Поиск путей в графе знаний
- Построение причинно-следственных цепочек
- Синтез выводов

**Пример:**
```python
from src.core.kag_solver import KAGSolver

solver = KAGSolver(graph_driver=driver, lm_client=client)
result = await solver.reason("Как работает RAG?")
```

### 2. Multi-Agent System

Система из 5 агентов:

#### Planner
Планирует стратегию рассуждения.

#### Researcher
Собирает данные из графа знаний.

#### Verifier
Проверяет достоверность рассуждений.

#### Writer
Составляет финальный отчёт.

#### Critic
Критически анализирует результаты.

### 3. Auto-Evaluator

Автоматическая самооценка системы.

**Метрики:**
- `accuracy_score` — точность рассуждений
- `confidence` — уверенность системы
- `staleness` — устаревание данных
- `ethical_violation_rate` — частота этических нарушений
- `trust` — общий уровень доверия

### 4. Ethical Filter

Этическая фильтрация контента.

**Проверки:**
- Токсичность
- Предвзятость
- Непроверяемые утверждения
- Заблокированные темы

### 5. Visualization

Визуализация reasoning путей.

**Форматы:**
- Интерактивный HTML (pyvis)
- Простой HTML (fallback)

---

## 🔄 Процесс работы

1. **Планирование** — Planner создаёт план рассуждения
2. **Исследование** — Researcher собирает данные из графа
3. **Рассуждение** — KAGSolver выполняет графовое рассуждение
4. **Проверка** — Verifier проверяет достоверность
5. **Этическая фильтрация** — EthicalFilter фильтрует контент
6. **Написание** — Writer составляет отчёт
7. **Критика** — Critic анализирует результат
8. **Оценка** — AutoEvaluator обновляет метрики
9. **Визуализация** — GraphDashboard создаёт визуализацию

---

## 📊 Результат

Результат работы Evolution Loop содержит:

```python
{
    "query": "Запрос пользователя",
    "timestamp": "2025-11-06T...",
    "stages": {
        "planning": {...},
        "research": {...},
        "reasoning": {...},
        "verification": {...},
        "ethical_filtering": {...},
        "writing": {...},
        "criticism": {...},
        "evaluation": {...}
    },
    "final_report": {
        "conclusion": "Вывод",
        "confidence": 0.85,
        "verification_status": "valid"
    },
    "visualization": "path/to/reasoning_paths.html",
    "duration_seconds": 12.5,
    "success": True,
    "system_health": {...}
}
```

---

## 🧪 Тестирование

Запустите тест:

```bash
python scripts/tests/test_evolution_loop.py
```

---

## 📈 Метрики и мониторинг

### Получение метрик

```python
metrics = loop.evaluator.get_metrics()
print(f"Accuracy: {metrics['accuracy']:.2f}")
print(f"Trust: {metrics['trust']:.2f}")
```

### Статус системы

```python
status = loop.get_system_status()
print(f"Health: {status['health']['status']}")
```

---

## 🔧 Конфигурация

### Этические правила

Создайте файл `data/ethics_rules.json`:

```json
{
    "check_toxicity": true,
    "check_bias": true,
    "check_unverifiable": true,
    "blocked_topics": [],
    "sensitivity_threshold": 0.7
}
```

---

## 🎯 Примеры использования

### Простой запрос

```python
result = await loop.run("Что такое RAG?")
print(result["final_report"]["conclusion"])
```

### С визуализацией

```python
result = await loop.run("Как работает TERAG?", visualize=True)
# Визуализация сохранена в data/reports/reasoning_paths_*.html
```

### Без визуализации

```python
result = await loop.run("Вопрос", visualize=False)
```

---

## 🔗 Интеграция

Evolution Loop интегрирован с:

- ✅ **Neo4j** — граф знаний
- ✅ **LM Studio** — reasoning backend
- ✅ **Graph Updater** — сохранение результатов
- ✅ **Pattern Memory** — обучение на паттернах
- ✅ **Learning Bridge** — двусторонний обмен знаниями

---

## 📝 Следующие шаги

1. **Улучшение KAG-Solver** — добавление более сложных алгоритмов рассуждения
2. **Расширение агентов** — добавление новых агентов (например, Summarizer)
3. **Улучшение визуализации** — интерактивные дашборды
4. **Интеграция с UI** — React-интерфейс для Cognitive Dashboard

---

**TERAG Evolution Loop — следующий уровень когнитивных возможностей TERAG!** 🧠✨













