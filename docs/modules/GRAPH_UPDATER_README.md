# 📊 TERAG Graph Updater — Интеграция с Neo4j

## ✅ Что уже сделано

- ✅ Метод `add_fact()` добавлен в `GraphUpdater`
- ✅ Метод `add_signal()` для добавления сигналов с novelty_score
- ✅ Метод `get_graph_stats()` для получения статистики графа
- ✅ Интеграция с `MissionRunner` — факты автоматически сохраняются в Neo4j
- ✅ Тесты работают — факты и сигналы успешно добавляются в граф
- ✅ Уведомления в Telegram (опционально)
- ✅ Prometheus метрики для мониторинга (опционально)

## 🚀 Быстрый старт

### 1. Проверка подключения

```bash
python test_neo4j_connection.py
```

Должно показать: `Neo4j connected ✅`

### 2. Тест добавления фактов

```bash
python scripts/tests/test_graph_updater_fact.py
```

Должно добавить 3 тестовых факта в граф.

### 3. Проверка в Neo4j Browser

Откройте http://localhost:7474 и выполните:

```cypher
MATCH (a)-[r]->(b) 
RETURN a, r, b 
LIMIT 10
```

Вы увидите факты в графе!

## 📝 Использование

### Прямое добавление факта

```python
from installer.app.modules.graph_updater import GraphUpdater

updater = GraphUpdater(
    uri="bolt://localhost:7687",
    user="neo4j",
    password="terag_neo4j_2025"
)

fact = {
    "subject": "AI",
    "predicate": "IMPACTS",
    "object": "Governance"
}

updater.add_fact(
    fact,
    source="https://example.com/article",
    confidence=0.92
)

updater.close()
```

### Автоматическое добавление через миссии

После запуска OSINT-миссии факты автоматически сохраняются:

```python
from installer.app.modules.mission_runner import MissionRunner

runner = MissionRunner("installer/data/mission.yaml")
result = runner.run_mission()  # Факты автоматически добавятся в граф
```

## 🔍 Структура графа

### Узлы (Nodes)

- **Entity** — концепты и сущности
  - `name` — название
  - `type` — тип (обычно "concept")
  - `created_at` — дата создания
  - `updated_at` — дата обновления

### Связи (Relationships)

- **RELATION** — связи между сущностями
  - `type` — тип связи (IMPACTS, RELATED_TO, REQUIRES, etc.)
  - `confidence` — уверенность (0.0-1.0)
  - `source` — URL источника
  - `uid` — уникальный идентификатор
  - `created_at` — дата создания

## 📊 API Reference

### `GraphUpdater.__init__(uri, user, password)`

Инициализация GraphUpdater.

**Параметры:**
- `uri` (str): URI Neo4j (например, "bolt://localhost:7687")
- `user` (str): Имя пользователя Neo4j
- `password` (str): Пароль Neo4j

**Пример:**
```python
updater = GraphUpdater("bolt://localhost:7687", "neo4j", "terag_neo4j_2025")
```

---

### `add_fact(fact, source="", confidence=1.0) -> bool`

Добавить факт в граф знаний (триплет: subject-predicate-object).

**Параметры:**
- `fact` (Dict[str, Any]): Словарь с полями:
  - `subject` (str): Субъект (источник связи)
  - `predicate` (str): Предикат (тип связи, по умолчанию "RELATED_TO")
  - `object` (str): Объект (цель связи)
- `source` (str, optional): URL источника факта
- `confidence` (float, optional): Уровень уверенности (0.0-1.0, по умолчанию 1.0)

**Возвращает:**
- `bool`: True если успешно, False иначе

**Пример:**
```python
fact = {
    "subject": "AI",
    "predicate": "IMPACTS",
    "object": "Governance"
}
updater.add_fact(fact, source="https://example.com/article", confidence=0.92)
```

---

### `add_signal(concept, domain, novelty_score, confidence) -> bool`

Добавить сигнал (новое открытие) в граф знаний.

**Параметры:**
- `concept` (str): Название концепта/сигнала
- `domain` (str): Домен (область знаний)
- `novelty_score` (float): Оценка новизны (0.0-1.0)
- `confidence` (float): Уровень уверенности (0.0-1.0)

**Возвращает:**
- `bool`: True если успешно, False иначе

**Пример:**
```python
updater.add_signal(
    concept="EdgeAI",
    domain="Semantic Intelligence",
    novelty_score=0.87,
    confidence=0.9
)
```

---

### `get_graph_stats() -> Dict[str, Any]`

Получить статистику графа знаний.

**Возвращает:**
- `Dict[str, Any]`: Словарь со статистикой:
  - `nodes` (int): Общее количество узлов
  - `relations` (int): Общее количество связей
  - `entities` (int): Количество Entity узлов
  - `signals` (int): Количество Signal узлов
  - `domains` (int): Количество Domain узлов
  - `error` (str, optional): Сообщение об ошибке, если произошла

**Пример:**
```python
stats = updater.get_graph_stats()
print(f"Узлов: {stats['nodes']}, Связей: {stats['relations']}")
```

---

### `close()`

Закрыть соединение с Neo4j.

**Пример:**
```python
updater.close()
```

---

## 📊 Примеры запросов

### Найти все факты о концепте

```cypher
MATCH (a:Entity {name: "AI"})-[r:RELATION]->(b:Entity)
RETURN a.name, r.type, b.name, r.confidence
```

### Найти все связи с высокой уверенностью

```cypher
MATCH (a:Entity)-[r:RELATION]->(b:Entity)
WHERE r.confidence > 0.8
RETURN a.name, r.type, b.name, r.confidence
ORDER BY r.confidence DESC
```

### Найти путь между концептами

```cypher
MATCH path = (a:Entity {name: "AI"})-[*..3]->(b:Entity {name: "Transparency"})
RETURN path
```

## 🔔 Уведомления в Telegram

При добавлении факта через миссию, TERAG автоматически отправляет уведомление:

```
🧩 Новый факт добавлен в граф

AI -[IMPACTS]-> Governance

Confidence: 0.92
Source: https://example.com/ai-governance
```

## 📈 Статистика

После выполнения миссии можно проверить статистику:

```python
result = runner.run_mission()
print(f"Facts added: {result['graph_update']['facts_added']}")
print(f"Total nodes: {result['graph_update']['nodes_count']}")
print(f"Total relationships: {result['graph_update']['relationships_count']}")
```

## 🧪 Тестирование

### Интеграционный тест

```bash
python scripts/tests/test_graph_updater_fact.py
```

Ожидаемый результат:
- ✅ 3 факта добавлены
- ✅ Узлов Entity: 5
- ✅ Связей RELATION: 3

## 📈 Мониторинг через Prometheus

Graph Updater поддерживает экспорт метрик в Prometheus для визуализации в Grafana.

### Установка

```bash
pip install prometheus-client
```

### Запуск метрик

```python
from src.core.graph_metrics import start_metrics_server

# Запустить HTTP сервер на порту 8000 (по умолчанию)
start_metrics_server()
```

Метрики будут доступны по адресу: `http://localhost:8000/metrics`

### Доступные метрики

- `terag_graph_facts_added_total` — общее количество добавленных фактов (по типу связи)
- `terag_graph_facts_failed_total` — общее количество ошибок добавления фактов
- `terag_graph_signals_added_total` — общее количество добавленных сигналов (по домену)
- `terag_graph_nodes_total` — текущее количество узлов (по типу)
- `terag_graph_relations_total` — текущее количество связей
- `terag_graph_entities_total` — текущее количество Entity узлов
- `terag_graph_signals_total` — текущее количество Signal узлов
- `terag_graph_domains_total` — текущее количество Domain узлов
- `terag_graph_fact_insertion_duration_seconds` — время вставки факта (гистограмма)
- `terag_graph_signal_insertion_duration_seconds` — время вставки сигнала (гистограмма)

### Настройка порта

Установите переменную окружения:
```bash
export PROMETHEUS_METRICS_PORT=9090
```

---

## 🧪 Нагрузочное тестирование

Для проверки производительности и конкурентной записи:

```bash
python scripts/tests/test_graph_updater_load.py
```

Тест проверяет:
- Последовательное добавление фактов (50 фактов)
- Последовательное добавление сигналов (20 сигналов)
- Конкурентную запись (5 потоков, 10 фактов на поток)

---

## 🔄 Следующие шаги

1. ✅ Graph Updater работает
2. ✅ Метрики Prometheus настроены
3. ⏭️ Запустите OSINT-миссию — факты автоматически добавятся
4. ⏭️ Проверьте граф в Neo4j Browser
5. ⏭️ Настройте Grafana дашборд для визуализации метрик
6. ⏭️ Используйте граф для KAG-Solver (задача 12)

---

**Готово!** TERAG теперь запоминает факты в графе знаний! 🎯✨

