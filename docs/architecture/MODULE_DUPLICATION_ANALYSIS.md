# 🔍 Анализ Дублирования Модулей TERAG

**Дата:** 2026-01-06  
**Статус:** В процессе анализа

---

## 📋 Обнаруженные Дублирования

### 1. KAG Builder - Три версии

#### 1.1 `src/core/kag_builder.py` ✅ **ОСНОВНОЙ**

**Назначение:** Основной модуль для построения графа знаний из документов

**Функциональность:**
- Конвертация документов (DOCX, PDF, XLSX)
- Извлечение SPO-триплетов
- Интеграция с Neo4j
- Сохранение результатов в `data/graph_results/`

**Использование:**
- Используется в основном API (`src/api/server.py`)
- Используется в тестах
- Используется в benchmark пайплайнах

**Статус:** ✅ **ОСНОВНОЙ МОДУЛЬ**

---

#### 1.2 `src/kag/builder.py` ⚠️ **СПЕЦИАЛИЗИРОВАННЫЙ**

**Назначение:** KAG Builder с интеграцией OpenSPG

**Функциональность:**
- Извлечение SPO-триплетов через OpenSPG
- Интеграция с Neo4j и Supabase
- Использование OpenSPG схемы для TERAG

**Отличия от основного:**
- Использует OpenSPG для структурирования графа
- Интегрирован с Supabase (Memory Bank)
- Более сложная архитектура

**Использование:**
- Планируется для будущей интеграции OpenSPG
- Пока не используется активно

**Статус:** ⚠️ **БУДУЩАЯ ИНТЕГРАЦИЯ** (можно оставить для будущего)

---

#### 1.3 `installer/app/modules/graph_updater.py` ⚠️ **МИССИОННЫЙ**

**Назначение:** Модуль для обновления графа в контексте Mission System

**Функциональность:**
- Обновление графа знаний из Mission YAML
- Интеграция с OSINT pipeline (Brave Search, Bright Data)
- Валидация данных через DeepConf

**Отличия:**
- Работает в контексте Mission System
- Интегрирован с OSINT компонентами
- Используется в `installer/app/main.py`

**Использование:**
- Используется в Docker контейнере (`installer/app/`)
- Используется в Mission Runner

**Статус:** ⚠️ **МИССИОННЫЙ МОДУЛЬ** (отдельный контекст использования)

---

### 2. Docker Compose - Четыре версии

#### 2.1 `docker-compose.yml` ✅ **ОСНОВНОЙ**

**Назначение:** Основная конфигурация для разработки

**Сервисы:**
- frontend (React + Vite)
- backend (FastAPI)
- neo4j

**Использование:** Основной файл для локальной разработки

---

#### 2.2 `docker-compose.kag.yml` ⚠️ **KAG СПЕЦИАЛИЗИРОВАННЫЙ**

**Назначение:** Конфигурация для KAG компонентов

**Отличия:** Добавлены сервисы для KAG pipeline

**Статус:** ⚠️ **СПЕЦИАЛИЗИРОВАННЫЙ** (можно объединить с основным)

---

#### 2.3 `docker-compose.kag-simple.yml` ⚠️ **УПРОЩЕННЫЙ**

**Назначение:** Упрощенная версия для тестирования

**Статус:** ⚠️ **ТЕСТОВЫЙ** (можно удалить или переименовать)

---

#### 2.4 `installer/docker-compose.yml` ✅ **ПРОДАКШН**

**Назначение:** Конфигурация для production deployment

**Сервисы:**
- terag-api (FastAPI)
- neo4j
- prometheus
- grafana

**Использование:** Используется в Docker контейнере

**Статус:** ✅ **ПРОДАКШН** (отдельный контекст)

---

### 3. API Servers - Две версии

#### 3.1 `src/api/server.py` ✅ **ОСНОВНОЙ**

**Назначение:** Основной API сервер для разработки

**Функциональность:**
- Все основные endpoints
- Интеграция с core модулями
- AI-REPS метрики

**Использование:** Локальная разработка, тестирование

**Статус:** ✅ **ОСНОВНОЙ**

---

#### 3.2 `src/api/terag_v2_server.py` ✅ **V2.0**

**Назначение:** API сервер версии 2.0

**Функциональность:**
- Новые endpoints `/api/v2/*`
- Интеграция с T.R.A.C.
- Поддержка новых роутеров

**Статус:** ✅ **НОВАЯ ВЕРСИЯ** (не дубликат, а эволюция)

---

#### 3.3 `installer/app/main.py` ⚠️ **МИССИОННЫЙ**

**Назначение:** API сервер для Mission System

**Функциональность:**
- Endpoints для Mission System
- Интеграция с `installer/app/modules/`
- Используется в Docker контейнере

**Статус:** ⚠️ **МИССИОННЫЙ** (отдельный контекст)

---

## 🎯 Рекомендации

### KAG Builder

**Решение:** Оставить все три версии, но четко документировать различия

1. **`src/core/kag_builder.py`** - основной для разработки
2. **`src/kag/builder.py`** - для будущей интеграции OpenSPG
3. **`installer/app/modules/graph_updater.py`** - для Mission System

**Действия:**
- ✅ Документировать различия
- ✅ Добавить комментарии в код
- ✅ Обновить документацию

---

### Docker Compose

**Решение:** Унифицировать структуру

1. **`docker-compose.yml`** - основной для разработки
2. **`docker-compose.prod.yml`** - переименовать `installer/docker-compose.yml`
3. **`docker-compose.kag.yml`** - объединить с основным или удалить
4. **`docker-compose.kag-simple.yml`** - удалить или переименовать в `docker-compose.test.yml`

**Действия:**
- Переименовать `installer/docker-compose.yml` → `docker-compose.prod.yml`
- Объединить или удалить специализированные версии
- Документировать различия

---

### API Servers

**Решение:** Четко разделить контексты использования

1. **`src/api/server.py`** - для разработки
2. **`src/api/terag_v2_server.py`** - новая версия API
3. **`installer/app/main.py`** - для Mission System (можно переименовать)

**Действия:**
- Документировать, когда использовать каждый
- Возможно, переименовать `installer/app/main.py` → `installer/app/mission_server.py`

---

## 📊 Итоговая Структура (Рекомендуемая)

```
TERAG111-1/
├── src/
│   ├── api/
│   │   ├── server.py              ✅ Основной API (dev)
│   │   └── terag_v2_server.py     ✅ API v2.0
│   ├── core/
│   │   └── kag_builder.py         ✅ Основной KAG Builder
│   └── kag/
│       └── builder.py              ⚠️ OpenSPG интеграция (будущее)
├── installer/
│   └── app/
│       ├── main.py                 ⚠️ Mission Server (можно переименовать)
│       └── modules/
│           └── graph_updater.py    ⚠️ Mission Graph Updater
└── docker-compose.yml              ✅ Основной (dev)
    docker-compose.prod.yml         ✅ Production (переименовать)
```

---

## ✅ План Действий

### Приоритет 1: Документация

- [ ] Создать `docs/architecture/MODULE_STRUCTURE.md` с описанием всех модулей
- [ ] Добавить комментарии в код о назначении каждого модуля
- [ ] Обновить `docs/INDEX.md` с ссылками на документацию

### Приоритет 2: Унификация Docker

- [ ] Переименовать `installer/docker-compose.yml` → `docker-compose.prod.yml`
- [ ] Объединить или удалить `docker-compose.kag*.yml`
- [ ] Документировать различия в `docs/deployment/`

### Приоритет 3: Переименование (опционально)

- [ ] Переименовать `installer/app/main.py` → `installer/app/mission_server.py`
- [ ] Обновить все ссылки

---

**Дата анализа:** 2026-01-06  
**Следующий шаг:** Документировать различия модулей
