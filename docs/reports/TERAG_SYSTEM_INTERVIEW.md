# 🧠 TERAG System Interview — Ответы на Архитектурные Вопросы

**Дата:** 2025-01-27  
**Основа:** Анализ реальной кодовой базы TERAG

---

## 📋 ПРЕДИСЛОВИЕ

Этот документ содержит ответы на 20 вопросов о TERAG, основанные на **реальном анализе кодовой базы**, а не на предположениях. Каждый ответ подкреплён ссылками на конкретные файлы и функции.

---

## 1️⃣ TERAG как программа: природа и назначение

### Вопрос 1: TERAG — это больше оркестратор, верификатор, интегратор, или execution runtime?

**Ответ:** **TERAG — это комбинация всех четырёх**, но с акцентом на **оркестратор** и **execution runtime**.

**Доказательства:**
- **Оркестратор:** `src/core/evolution_loop.py` — координирует работу агентов (Planner → Researcher → Solver → Verifier → Writer → Critic)
- **Верификатор:** `src/core/agents/verifier.py` — проверяет достоверность рассуждений
- **Интегратор:** `src/core/ingest_pipeline.py` — интегрирует файлы, граф, LLM, метрики
- **Execution Runtime:** `src/core/agents/langgraph_core.py` — StateGraph для выполнения reasoning pipeline

**Вывод:** TERAG — это **когнитивный оркестратор** с встроенными верификацией и интеграцией.

---

### Вопрос 2: Он работает как CLI-утилита, desktop-приложение, серверная система, API, или фреймворк?

**Ответ:** **TERAG — это серверная система с API**, но может работать и как CLI.

**Доказательства:**
- **API сервер:** `src/api/server.py` — FastAPI приложение на порту 8000
- **CLI утилиты:** 
  - `ask_rag_v2.py` — CLI для RAG поиска
  - `TERAG_ROOT/process_inbox_now.py` — CLI для обработки файлов
  - `TERAG_ROOT/scripts/watch_and_ingest.py` — CLI для мониторинга
- **Web UI:** `src/api/static/index.html` — интерактивный дашборд
- **3D UI:** `src/components/immersive/` — React компоненты для Immersive Shell

**Вывод:** TERAG — **серверная система (FastAPI)** с веб-интерфейсом, но может использоваться как CLI.

---

### Вопрос 3: TERAG может работать в автономном режиме, или он требует постоянного подключения к сервисам?

**Ответ:** **TERAG может работать автономно**, но с ограничениями.

**Доказательства:**
- **Локальные зависимости:**
  - Neo4j — локальная БД (bolt://localhost:7687)
  - ChromaDB — локальная векторная БД
  - Ollama/LM Studio — локальные LLM модели
- **Опциональные внешние сервисы:**
  - Brave Search API (для OSINT) — опционально
  - Bright Data (для скрапинга) — опционально
  - Telegram Bot — опционально
- **Safe Mode:** `src/core/config.py` — режим, ограничивающий внешние запросы

**Вывод:** TERAG **работает автономно** для базовых функций (обработка файлов, Graph-RAG, reasoning). Внешние API нужны только для OSINT-функций.

---

## 2️⃣ Структура данных и стандарты

### Вопрос 4: Какие типы данных TERAG изначально умеет обрабатывать?

**Ответ:** TERAG обрабатывает **текстовые форматы** и **структурированные данные**.

**Доказательства:**
- **Файлы:** `src/core/doc_converter.py`, `src/core/ingest_pipeline.py`
  - ✅ PDF (PyPDF2, pdfplumber)
  - ✅ DOCX (python-docx)
  - ✅ XLSX/XLS (pandas)
  - ✅ HTML/HTM (BeautifulSoup)
  - ✅ TXT, MD, JSON
  - ✅ ZIP (рекурсивная распаковка)
- **Структурированные данные:**
  - ✅ JSON — через стандартный парсер
  - ✅ YAML — через Mission YAML (`installer/data/mission.yaml`)
  - ❌ GeoTIFF — не поддерживается
  - ❌ COG — не поддерживается
  - ❌ Parquet — не поддерживается
  - ❌ Raster stacks — не поддерживается

**Вывод:** TERAG работает с **текстовыми и офисными форматами**. Геопространственные форматы (GeoTIFF, COG) **не поддерживаются**.

---

### Вопрос 5: Есть ли у TERAG понятие schema, profile, manifest, integrity hash?

**Ответ:** **Да, частично.**

**Доказательства:**
- **Schema:** `src/kag/schemas/terag_schema.py` — формальная схема OpenSPG с валидацией
- **Manifest:** 
  - Mission YAML (`installer/data/mission.yaml`) — манифест миссий
  - ETL manifest — через MCP (`mcp_geoscan-core_get_satellite_etl_manifest_tool`)
- **Integrity hash:**
  - ✅ MD5 для кэширования (`src/api/server.py:308`, `src/core/cache.py:60`)
  - ✅ SHA-256 для trace (`src/utils/trace.ts:21`, `scripts/supabase-integration.js:273`)
  - ⚠️ Нет проверки целостности файлов
- **Profile:** ❌ Нет концепта профилей данных

**Вывод:** TERAG имеет **schema** и **manifest**, но **нет профилей** и **проверки целостности файлов**.

---

### Вопрос 6: Может ли TERAG выполнять валидацию входящих данных?

**Ответ:** **Да, но базовая.**

**Доказательства:**
- **Валидация схемы:** `src/kag/schemas/terag_schema.py:621` — `validate_entity()`, `validate_relation()`
- **Валидация структуры:** 
  - Проверка обязательных полей
  - Проверка типов данных
  - Проверка constraints
- **Валидация формата:** 
  - Проверка расширений файлов
  - Проверка кодировки (UTF-8, CP1251)
- **Ограничения:**
  - ❌ Нет проверки полей GeoTIFF
  - ❌ Нет проверки структуры COG
  - ❌ Нет проверки метаданных растров

**Вывод:** TERAG валидирует **текстовые данные и схемы графа**, но **не валидирует геопространственные форматы**.

---

## 3️⃣ TERAG и процессы

### Вопрос 7: TERAG умеет исполнять pipeline (например DAG/steps)?

**Ответ:** **Да, через Mission YAML и LangGraph.**

**Доказательства:**
- **Mission YAML:** `installer/data/mission.yaml` — декларативное описание pipeline
  - Компоненты: `brave_search`, `bright_extraction`, `deepconf_validation`, `graph_rag_update`
  - Последовательность шагов
- **LangGraph:** `src/core/agents/langgraph_core.py` — StateGraph для reasoning pipeline
  - START → Guardrail → Planner → Solver → Verifier → END
- **Benchmark pipelines:** `src/benchmark/pipelines/` — Vector-RAG, Graph-RAG, Hybrid-RAG

**Вывод:** TERAG **исполняет pipeline** через Mission YAML и LangGraph StateGraph.

---

### Вопрос 8: Может ли TERAG ждать события, запускаться по расписанию, запускаться по триггеру?

**Ответ:** **Да, все три варианта.**

**Доказательства:**
- **По расписанию:**
  - `TASK_SCHEDULER_SETUP.md` — Windows Task Scheduler
  - `src/integration/telegram_service.py:501` — `schedule_daily_report()` через APScheduler
  - Mission YAML: `schedule: "daily"`, `schedule_config.daily_run_time: "02:00"`
- **По триггеру (файлы):**
  - `TERAG_ROOT/scripts/watch_and_ingest.py` — мониторинг папки `inbox/`
  - `scripts/auto_watcher.py` — FileSystemEventHandler для новых файлов
- **События:**
  - Telegram команды (`src/integration/telegram_service.py`)
  - API endpoints (`src/api/server.py`)

**Вывод:** TERAG **поддерживает все три режима** запуска.

---

### Вопрос 9: Способен ли TERAG работать в режиме сертификации или «подписи» того, что данные корректны?

**Ответ:** **Частично — есть концепт, но нет полной реализации.**

**Доказательства:**
- **Verifier Agent:** `src/core/agents/verifier.py` — проверяет достоверность
- **CoVe (Chain of Verification):** `.cursor/validation/cove-config.yaml` — конфигурация проверок
- **Confidence scores:** везде в системе (confidence, novelty_index, signal_strength)
- **Ограничения:**
  - ❌ Нет цифровых подписей
  - ❌ Нет криптографической сертификации
  - ❌ Нет "паспорта данных" с подписью

**Вывод:** TERAG **проверяет корректность** через Verifier, но **не выдаёт криптографические сертификаты**.

---

## 4️⃣ TERAG и T-Rack

### Вопрос 10: TERAG может управлять процессами T-Rack, запускать конвертацию, инициировать архивацию, проверять результат T-Rack?

**Ответ:** **Нет, в текущей реализации TERAG не интегрирован с T-Rack.**

**Доказательства:**
- Поиск по коду: нет упоминаний "t-rack", "track", "конвертация", "архивация"
- TERAG работает с файлами через `ingest_pipeline.py`, но не управляет внешними процессами
- Нет модулей для управления процессами T-Rack

**Вывод:** TERAG **не управляет T-Rack** в текущей версии. Это нужно **добавить**.

---

### Вопрос 11: Каким образом TERAG общается с T-Rack: через файлы? RPC? HTTP? Plugin API?

**Ответ:** **Не реализовано, но логично через файлы или HTTP API.**

**Рекомендация:**
- **Файлы:** TERAG может мониторить папку, куда T-Rack кладёт результаты
- **HTTP API:** TERAG может вызывать T-Rack через REST API (если T-Rack его предоставляет)
- **RPC/Plugin:** нужно реализовать

**Вывод:** Интеграция с T-Rack **не реализована**, но может быть добавлена через файлы или HTTP.

---

## 5️⃣ TERAG и GeoScan

### Вопрос 12: В какой момент TERAG должен подключаться в GeoScan pipeline?

**Ответ:** **Не реализовано, но логично после ETL и перед/вместо QA.**

**Анализ:**
- TERAG имеет MCP интеграцию с GeoScan (`mcp_geoscan-core_*` функции)
- TERAG может валидировать данные через Verifier
- TERAG может строить граф знаний из результатов ETL

**Рекомендация:**
```
GeoScan ETL → TERAG Validation → TERAG Graph-RAG → DataCube
```

**Вывод:** Интеграция с GeoScan **частично есть** (MCP), но полный pipeline **не реализован**.

---

### Вопрос 13: TERAG должен принимать решения о валидности данных, переходе на следующую страну, истинной версии слоя, политике хранения?

**Ответ:** **Частично — может принимать решения о валидности, но не о политике хранения.**

**Доказательства:**
- **Валидность:** ✅ Verifier Agent проверяет данные
- **Переход на следующую страну:** ❌ Не реализовано
- **Истинная версия слоя:** ❌ Не реализовано
- **Политика хранения:** ❌ Не реализовано

**Вывод:** TERAG **может валидировать**, но **не управляет workflow GeoScan**.

---

## 6️⃣ TERAG compute-возможности

### Вопрос 14: TERAG способен выполнять вычисления, или он только маршрутизатор?

**Ответ:** **TERAG выполняет вычисления, но ограниченные.**

**Доказательства:**
- **LLM reasoning:** `src/core/kag_solver/solver.py` — логические выводы
- **Эмбеддинги:** `src/core/ingest_pipeline.py:228` — генерация эмбеддингов
- **Метрики:** `src/core/metrics.py` — вычисление AI-REPS метрик
- **Графовые вычисления:** Neo4j Cypher запросы
- **Ограничения:**
  - ❌ Нет геопространственных вычислений
  - ❌ Нет обработки растров
  - ❌ Нет конвертации форматов

**Вывод:** TERAG **выполняет когнитивные вычисления** (reasoning, эмбеддинги), но **не геопространственные**.

---

### Вопрос 15: Может ли TERAG перепаковать файл, конвертировать формат, создавать COG, анализировать метаданные?

**Ответ:** **Частично — только текстовые форматы.**

**Доказательства:**
- **Конвертация:** ✅ `src/core/doc_converter.py` — PDF/DOCX/XLSX → TXT
- **Метаданные:** ✅ Извлечение метаданных из файлов (язык, размер, количество чанков)
- **Переупаковка:** ❌ Не реализовано
- **COG:** ❌ Не поддерживается
- **Геопространственные форматы:** ❌ Не поддерживаются

**Вывод:** TERAG **конвертирует текстовые форматы** и **анализирует метаданные**, но **не работает с геопространственными данными**.

---

## 7️⃣ TERAG как доверенный слой

### Вопрос 16: TERAG использует криптографию, подписи, контроль целостности?

**Ответ:** **Частично — есть хэши, но нет криптографии.**

**Доказательства:**
- **Хэши:**
  - ✅ MD5 для кэширования (`src/api/server.py:308`)
  - ✅ SHA-256 для trace (`src/utils/trace.ts:21`)
- **Криптография:** ❌ Нет цифровых подписей
- **Контроль целостности:** ❌ Нет проверки целостности файлов

**Вывод:** TERAG использует **хэши для внутренних целей**, но **не криптографию** и **не контроль целостности**.

---

### Вопрос 17: Есть ли у TERAG концепт versioned evidence или certified outputs?

**Ответ:** **Частично — есть версионирование, но нет сертификации.**

**Доказательства:**
- **Версионирование:**
  - Mission YAML: `version: "1.0.0"`
  - Graph версии: упоминается в документации, но не реализовано
- **Certified outputs:** ❌ Нет

**Вывод:** TERAG имеет **базовое версионирование**, но **нет сертифицированных выходов**.

---

### Вопрос 18: Может ли TERAG выдавать «паспорт данных»?

**Ответ:** **Нет, но может быть добавлен.**

**Текущее состояние:**
- TERAG сохраняет метаданные в Neo4j
- TERAG имеет confidence scores
- Но нет формального "паспорта данных"

**Рекомендация:** Добавить генерацию JSON-паспорта:
```json
{
  "dataset": "kz_landsat_tile_042",
  "validated": true,
  "hash": "sha256:...",
  "source": "USGS",
  "converted_by": "t-rack",
  "terag_signature": "...",
  "confidence": 0.87,
  "processed_at": "2025-01-27T12:00:00Z"
}
```

**Вывод:** **Не реализовано**, но **может быть добавлено**.

---

## 8️⃣ TERAG интерфейсы

### Вопрос 19: У него есть UI или он чисто программный?

**Ответ:** **Есть и UI, и программный интерфейс.**

**Доказательства:**
- **Web UI:** `src/api/static/index.html` — интерактивный дашборд
- **3D UI:** `src/components/immersive/` — Immersive Shell с 3D визуализацией
- **API:** `src/api/server.py` — REST API
- **CLI:** `ask_rag_v2.py`, `process_inbox_now.py`
- **Telegram:** `src/integration/telegram_service.py` — бот интерфейс

**Вывод:** TERAG имеет **множественные интерфейсы** — Web, 3D, API, CLI, Telegram.

---

### Вопрос 20: Он ожидает ручного вмешательства, или полностью автоматический?

**Ответ:** **Оба режима поддерживаются.**

**Доказательства:**
- **Автоматический:**
  - `watch_and_ingest.py` — автоматическая обработка файлов
  - Task Scheduler — автозапуск
  - Telegram бот — автоматические отчёты
- **Ручной:**
  - Web UI — интерактивный чат
  - CLI — ручной запуск команд
  - API — программный доступ

**Вывод:** TERAG **поддерживает оба режима** — автоматический и ручной.

---

## 📊 ИТОГОВАЯ ТАБЛИЦА ОТВЕТОВ

| Вопрос | Ответ | Статус | Примечание |
|--------|-------|--------|------------|
| 1. Природа TERAG | Оркестратор + Runtime | ✅ | Комбинация всех ролей |
| 2. Тип системы | Серверная (FastAPI) + CLI | ✅ | Множественные интерфейсы |
| 3. Автономность | Да, с ограничениями | ✅ | Локальные LLM, БД |
| 4. Типы данных | Текстовые форматы | ⚠️ | Нет GeoTIFF/COG |
| 5. Schema/Manifest | Да, частично | ✅ | Schema есть, профилей нет |
| 6. Валидация | Базовая | ⚠️ | Только текстовые данные |
| 7. Pipeline | Да | ✅ | Mission YAML + LangGraph |
| 8. Расписание/Триггеры | Да | ✅ | Все три режима |
| 9. Сертификация | Частично | ⚠️ | Verifier есть, подписей нет |
| 10. T-Rack управление | Нет | ❌ | Не реализовано |
| 11. T-Rack коммуникация | Не реализовано | ❌ | Можно добавить |
| 12. GeoScan интеграция | Частично | ⚠️ | MCP есть, pipeline нет |
| 13. Решения GeoScan | Частично | ⚠️ | Только валидация |
| 14. Вычисления | Да, ограниченные | ⚠️ | Когнитивные, не гео |
| 15. Конвертация | Текстовые форматы | ⚠️ | Нет COG/GeoTIFF |
| 16. Криптография | Хэши есть | ⚠️ | Нет подписей |
| 17. Versioned evidence | Частично | ⚠️ | Версии есть, сертификации нет |
| 18. Паспорт данных | Нет | ❌ | Можно добавить |
| 19. UI | Да | ✅ | Web, 3D, API, CLI, Telegram |
| 20. Автоматизация | Да | ✅ | Оба режима |

---

## 🎯 ВЫВОДЫ ДЛЯ ИНТЕГРАЦИИ С GEOSCAN

### ✅ Что TERAG может делать СЕЙЧАС:

1. **Валидировать данные** через Verifier Agent
2. **Строить граф знаний** из результатов ETL
3. **Обрабатывать файлы** (текстовые форматы)
4. **Работать автоматически** через watch & ingest
5. **Выдавать метрики** качества (AI-REPS)

### ❌ Что нужно ДОБАВИТЬ:

1. **Интеграция с T-Rack:**
   - Мониторинг папки результатов T-Rack
   - Вызов T-Rack API (если есть)
   - Проверка результатов конвертации

2. **Геопространственные форматы:**
   - Поддержка GeoTIFF
   - Поддержка COG
   - Анализ метаданных растров

3. **Паспорт данных:**
   - Генерация JSON-паспорта
   - Криптографические подписи (опционально)
   - Версионирование слоёв

4. **GeoScan pipeline:**
   - Интеграция после ETL
   - Решения о валидности
   - Управление workflow

---

## 📝 РЕКОМЕНДАЦИИ

### Приоритет 1: Интеграция с T-Rack
- Добавить модуль `src/integration/track_integration.py`
- Мониторинг папки результатов
- Проверка результатов конвертации

### Приоритет 2: Геопространственные форматы
- Добавить поддержку GeoTIFF через `rasterio`
- Анализ метаданных COG
- Валидация геопространственных данных

### Приоритет 3: Паспорт данных
- Генерация JSON-паспорта
- Интеграция с Verifier
- Сохранение в граф знаний

### Приоритет 4: GeoScan pipeline
- Полная интеграция через MCP
- Управление workflow
- Решения о переходе на следующую стадию

---

**Версия:** 1.0  
**Дата:** 2025-01-27  
**Основа:** Анализ кодовой базы TERAG


