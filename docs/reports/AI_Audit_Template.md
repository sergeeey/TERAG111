# 🧭 Auditor CurSor Unified AI-Audit Spec v1.0

**Цель:** провести воспроизводимую, измеримую и саморефлексивную проверку AI-системы перед промышленным использованием.  
**Применение:** AI-native проекты — LLM-агенты, ML-модели, когнитивные пайплайны, мультиагентные системы.  
**Стандарты:** COBIT 2019, ISO 27001, ISO 42010, ISO 12207, ISO 25010, ITAF 4.0, DORA, OWASP Top-10 for LLM, CSA 2.0, NIST AI RMF.

---

## 💠 0. Подготовка среды

```bash
git clone <repository_url> project && cd project
pip install -r requirements.txt
pip install bandit safety pytest pydeps
```

Создать `.auditconfig.yaml`:

```yaml
audit_level: L1 | L2 | L3
project_name: "MyProject"
source_dirs: ["src/", "core/"]
test_dir: "tests/"
thresholds:
  coverage: 80
  security_issues: 5
  coupling: 0.4
```

Запуск сбора данных:

```bash
make audit-dependencies
```

---

## 🚀 1. Уровни аудита

| Уровень | Время   | Цель                                                               |
| ------- | ------- | ------------------------------------------------------------------ |
| **L1**  | ~15 мин | Проверка базовой жизнеспособности (структура, тесты, безопасность) |
| **L2**  | ~1–2 ч  | Техническая готовность к развёртыванию                             |
| **L3**  | ~4+ ч   | Когнитивная и операционная зрелость, готовность к сертификации     |

---

## 🧱 2. Архитектура (ISO 42010 / COBIT)

```bash
pydeps --show-dot --no-output --display=pydeps.svg src/
```

**Метрики:**

* `architecture_score = (has_src*0.4) + (has_tests*0.4) + (1–coupling)*0.2`
* `coupling_coefficient`
* `maintainability_index`

**Критерии:** модульность, прослеживаемость, документированность, контроль зависимостей.

---

## 🔐 3. Безопасность (ISO 27001 / OWASP for LLM)

```bash
bandit -r src/ -f json -o bandit_report.json
safety check -r requirements.txt --json > safety_report.json
```

**Метрики:**

* `security_compliance_ratio = 1 – (0.2*high + 0.05*medium)`
* `critical_vulnerabilities_count`
* `risk_level`

**Критерии:** безопасность кода, управление зависимостями, защита от prompt injection и data leakage.

---

## 🧪 4. Тестирование и верификация (ISO 9001 / ITAF)

```bash
pytest --cov=src --cov-report=xml:coverage.xml tests/
```

**Метрики:**

* `coverage (%)`
* `verification_confidence = passed / total`
* `ci_pipeline_stability`

**Цель:** доказуемое качество кода и достоверность вывода модели (faithfulness).

---

## ⚙️ 5. Производительность и устойчивость (DORA)

```bash
pytest --benchmark-only --benchmark-json=perf_report.json tests/benchmarks/
```

**Метрики:**

* `performance_score`
* `reliability_under_load`
* `optimization_gain`

**Оценка:** стабильность под нагрузкой, эффективность вычислений, соответствие SLA/SLO.

---

## 🛰 6. Операционная готовность (ISO 12207)

**Проверить:** `Dockerfile`, `.env.example`, `docker-compose.yml`, мониторинг, CI/CD.

**Метрики:**

* `operational_readiness_index = (has_dockerfile*0.4) + (has_env*0.3) + (has_monitoring*0.3)`
* `deployment_confidence`
* `user_acceptance_success_rate`

---

## 🧠 7. Когнитивная логика и саморефлексия (CSA 2.0)

**Действия:**

* анализировать `reasoning_traces.log`
* выявить циклы **Observe → Decide → Act**

**Метрики:**

* `cognitive_consistency = 1 – (redundant_steps / total_steps)`
* `rss (Reflective Stability Score)`
* `cos (Counterfactual Soundness)`
* `self_validation_level (1–5)`

---

## 📊 8. Данные и прослеживаемость (ISO 25010)

**Проверить:**

* наличие версий моделей (DVC, Git-LFS)
* explainability-отчёты (SHAP, LIME)

**Метрики:**

* `traceability = (has_data_versioning + has_explainability) / 2`
* `transparency (%)`

---

## 🔁 9. Drift и временная устойчивость

**Действия:** анализировать точность и производительность во времени, проводить A/B-тесты.

**Метрики:**

* `drift_index`
* `rss_delta`
* `stability_trend`

---

## 📋 10. Контекст и миссия (AI Governance Layer)

Формулировка для системы:

> «Я — [name], создан(а) для […]. Моя цель — […]. Успех измеряю […].»

**Метрики:**

* `clarity`
* `contextual_maturity`
* `confidence`

---

## 🧾 11. Формат JSON-отчёта

```json
{
  "audit_id": "uuid-v4",
  "project_name": "MyProject",
  "audit_level": "L3",
  "scores": {
    "overall": 0.91,
    "architecture": 0.9,
    "security": 0.88,
    "coverage": 85,
    "performance": 0.92,
    "rss": 0.85,
    "traceability": 0.95,
    "operational_readiness": 0.92
  },
  "summary": {
    "strengths": ["чистая архитектура", "высокая устойчивость"],
    "weaknesses": ["недостаточная self-validation"],
    "recommendations": ["добавить контрфактические проверки"]
  }
}
```

---

## ⚡ 12. Интеграция с Cursor и CI/CD

```yaml
audit:
  run_on_pr: true
  audit_level: L1
  fail_on_threshold_breach: true
  block_merge_on_score_below: 0.7
```

Хранить отчёты под ключом
`audit_history:<project_name>:<timestamp>`

---

## 🧮 13. Интерпретация результатов

| Балл     | Статус | Интерпретация                        |
| -------- | ------ | ------------------------------------ |
| 0–0.59   | 🔴     | Требуется реструктуризация           |
| 0.6–0.79 | 🟡     | Работоспособно, но требует улучшений |
| 0.8–1.0  | 🟢     | Готово к production и сертификации   |

---

## 🪞 14. Завершение и прогноз

Система описывает:

* как выполняет миссию;
* какие улучшения повысят эффективность;
* прогноз стабильности, RSS и производительности на следующий цикл.

---

## 📦 Выходные артефакты аудита

* `final_audit_report.json` — цифровой отчёт
* `Cognitive_Audit_Card.md` — карта когнитивного состояния
* `POA&M.xlsx` — план корректирующих действий
* `Audit_Maturity_Model.md` — уровень зрелости проекта

---

**Auditor CurSor Unified Spec v1.0**
*Framework of reproducible, measurable, and self-reflective AI audits.*









































