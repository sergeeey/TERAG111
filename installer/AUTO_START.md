# 🚀 TERAG Автозапуск

TERAG теперь может автоматически запускаться при загрузке системы через Windows Task Scheduler.

## 📋 Быстрый старт

### Включить автозапуск

```powershell
# Запусти PowerShell от имени администратора
cd D:\TERAG111-1\installer
.\rebuild_api.ps1 -InstallPath "E:\TERAG" -EnableAutoStart
```

После этого TERAG будет автоматически запускаться при каждой загрузке системы.

### Отключить автозапуск

```powershell
.\rebuild_api.ps1 -InstallPath "E:\TERAG" -DisableAutoStart
```

## 🔍 Проверка статуса

### Через Task Scheduler

1. Открой `taskschd.msc` (или найдите "Планировщик заданий" в меню Пуск)
2. Перейдите в **Библиотека планировщика заданий**
3. Найдите задачу **"TERAG Auto Start"**

### Через PowerShell

```powershell
Get-ScheduledTask -TaskName "TERAG Auto Start"
```

## ⚙️ Как это работает

### Что делает задача

1. **Триггер**: Запускается при загрузке системы
2. **Действие**: Выполняет `docker compose up -d` в директории установки TERAG
3. **Права**: Запускается от имени SYSTEM с наивысшими привилегиями
4. **Поведение**: 
   - Работает даже при питании от батареи (для ноутбуков)
   - Автоматически перезапускается при сбое (до 3 раз с интервалом 1 минута)

### Команда, которая выполняется

```powershell
Set-Location 'E:\TERAG'
docker compose --env-file config.env up -d
```

## 🛠️ Ручная настройка (если скрипт не работает)

Если автоматическая регистрация не сработала, можно создать задачу вручную:

1. Открой **Task Scheduler** (`taskschd.msc`)
2. Нажми **Создать задачу** (не "Создать простую задачу")
3. **Общие**:
   - Имя: `TERAG Auto Start`
   - Отметь: "Выполнять с наивысшими правами"
   - Настройка: "Windows 10" или "Windows Server 2016"
4. **Триггеры**:
   - Нажми **Создать**
   - Начало задачи: **При запуске компьютера**
   - Отметь: **Включено**
5. **Действия**:
   - Действие: **Запустить программу**
   - Программа/скрипт: `powershell.exe`
   - Аргументы:
     ```
     -NoProfile -ExecutionPolicy Bypass -Command "Set-Location 'E:\TERAG'; docker compose --env-file config.env up -d"
     ```
6. **Условия**:
   - Отключи "Запускать только при питании от сети" (если ноутбук)
7. **Параметры**:
   - Отметь "Разрешить запуск по требованию"
   - Отметь "Если выполнение задачи не удалось, запускать повторно"
   - Интервал: **1 минута**
   - Попыток: **3**

## 🐛 Устранение проблем

### Задача не запускается

1. **Проверь права администратора**:
   ```powershell
   ([Security.Principal.WindowsPrincipal] [Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
   ```
   Должно вернуть `True`

2. **Проверь логи Task Scheduler**:
   - Открой Task Scheduler
   - Найди задачу "TERAG Auto Start"
   - Перейди на вкладку **Журнал**
   - Проверь последние выполнения

3. **Проверь путь к Docker**:
   ```powershell
   docker --version
   docker compose version
   ```
   Если команды не работают, добавь Docker в PATH или используй полный путь.

### Контейнеры не стартуют

1. **Проверь вручную**:
   ```powershell
   cd E:\TERAG
   docker compose --env-file config.env up -d
   ```

2. **Проверь логи**:
   ```powershell
   docker compose logs
   ```

3. **Проверь config.env**:
   Убедись, что файл `config.env` существует и содержит правильные настройки.

### Задача создана, но контейнеры не запускаются

1. **Проверь путь в задаче**:
   - Открой Task Scheduler
   - Найди задачу "TERAG Auto Start"
   - Проверь аргументы команды
   - Убедись, что путь `E:\TERAG` правильный

2. **Запусти задачу вручную**:
   - В Task Scheduler нажми правой кнопкой на задачу
   - Выбери **Выполнить**
   - Проверь результат

3. **Проверь переменные окружения**:
   Убедись, что Docker доступен в системном PATH.

## 📊 Мониторинг

После включения автозапуска, TERAG будет доступен сразу после загрузки системы:

- **API**: http://localhost:8000/health
- **Grafana**: http://localhost:3000
- **Prometheus**: http://localhost:9090
- **Neo4j**: http://localhost:7474

Проверь статус через несколько минут после загрузки:

```powershell
Invoke-RestMethod -Uri "http://localhost:8000/health" | ConvertTo-Json -Depth 5
```

## 🔗 Связанные документы

- [INSTALL.md](INSTALL.md) - Полная инструкция по установке
- [QUICK_START.md](QUICK_START.md) - Быстрый старт
- [README.md](README.md) - Общая информация о TERAG


















