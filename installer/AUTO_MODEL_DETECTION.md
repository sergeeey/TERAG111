# 🤖 Автоматическое определение моделей Ollama

## Обзор

TERAG теперь автоматически определяет и выбирает лучшую доступную модель Ollama при запуске, если модель не указана в `config.env` или если указанная модель недоступна.

## 🎯 Как это работает

### Приоритет моделей

Система использует следующий приоритет при выборе модели:

1. **qwen3-coder** - лучшая для задач анализа кода
2. **qwen2.5-coder** - альтернатива для кода
3. **deepseek-coder** - специализированная модель для программирования
4. **llama3** - универсальная модель общего назначения
5. **phi3** - легковесная модель
6. **mistral** - универсальная модель
7. **gemma** - модель от Google
8. **codellama** - специализированная для кода

### Логика выбора

1. Если в `config.env` указана модель (`LLM_MODEL=qwen3-coder`) и она доступна → используется она
2. Если указанная модель недоступна → система ищет похожую модель (например, `qwen3-coder` → `qwen3-coder:latest`)
3. Если модель не указана → выбирается лучшая доступная по приоритету
4. Если ничего не найдено → используется первая доступная модель

## 📝 Примеры конфигурации

### Вариант 1: Автоматический выбор (рекомендуется)

```env
LLM_PROVIDER=ollama
LLM_URL=http://host.docker.internal:11434
# LLM_MODEL не указан - система выберет лучшую доступную
```

### Вариант 2: Указать предпочтительную модель

```env
LLM_PROVIDER=ollama
LLM_URL=http://host.docker.internal:11434
LLM_MODEL=qwen3-coder
```

Если `qwen3-coder` недоступен, система автоматически выберет похожую модель (например, `qwen3-coder:latest` или приоритетную альтернативу).

### Вариант 3: Полное имя модели

```env
LLM_PROVIDER=ollama
LLM_URL=http://host.docker.internal:11434
LLM_MODEL=qwen3-coder:latest
```

## 🔍 Проверка автоопределения

### 1. Проверить текущую модель

```powershell
curl http://localhost:8000/llm/health
```

Ответ покажет:
```json
{
  "status": "healthy",
  "provider": "ollama",
  "base_url": "http://host.docker.internal:11434",
  "current_model": "qwen3-coder:latest",
  "preferred_model": "qwen3-coder",
  "auto_detected": true,
  "available_models_count": 5,
  "available_models": ["qwen3-coder:latest", "llama3:8b", ...]
}
```

### 2. Список всех доступных моделей

```powershell
curl http://localhost:8000/llm/models
```

### 3. Логи при запуске

При запуске контейнера в логах будет видно:

```
INFO: Auto-detected model: qwen3-coder:latest (preferred: qwen3-coder)
INFO: LLM client initialized: ollama at http://host.docker.internal:11434 with model qwen3-coder:latest
```

## 🚀 Использование

После перезапуска контейнера система автоматически:

1. Подключится к Ollama
2. Получит список доступных моделей
3. Выберет лучшую модель по приоритету
4. Готова к работе!

```powershell
# Перезапустить API для применения автоопределения
cd E:\TERAG
docker compose restart terag-api

# Проверить результат
curl http://localhost:8000/llm/health
```

## 💡 Преимущества

- ✅ **Автоматизация** - не нужно вручную указывать модель
- ✅ **Гибкость** - если модель недоступна, система выберет альтернативу
- ✅ **Умный выбор** - приоритет моделей оптимизирован для задач TERAG
- ✅ **Прозрачность** - всегда видно, какая модель используется

## 🔧 Расширенные настройки

Если нужно изменить приоритет моделей, отредактируйте функцию `_detect_best_model()` в `modules/llm_client.py`:

```python
priority_models = [
    "qwen3-coder",      # Ваша модель с высшим приоритетом
    "your-custom-model",
    "llama3",
    # ...
]
```

## 📚 Связанные документы

- [LLM Integration Guide](LLM_INTEGRATION.md)
- [Idea Collector Guide](IDEA_COLLECTOR_GUIDE.md)
- [TERAG API Documentation](http://localhost:8000/docs)



















