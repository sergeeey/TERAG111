# 🚀 TERAG Local Installer - Инструкция по установке

## Быстрый старт

### Шаг 1: Подготовка

1. Убедитесь, что установлен **Docker Desktop** и он запущен
2. Проверьте, что диск **E:** доступен и имеет свободное место (минимум 4GB)

### Шаг 2: Запуск установки

Откройте PowerShell от имени администратора и выполните:

```powershell
cd installer
powershell -ExecutionPolicy Bypass -File .\setup_terag.ps1
```

Или с указанием другого пути:

```powershell
powershell -ExecutionPolicy Bypass -File .\setup_terag.ps1 -InstallPath "E:\TERAG"
```

### Шаг 3: Ожидание запуска

После выполнения скрипта подождите 30-60 секунд для полной инициализации всех сервисов.

## Проверка установки

### Проверка сервисов

Откройте в браузере:

- **TERAG API:** http://localhost:8000
- **API Docs:** http://localhost:8000/docs
- **Health Check:** http://localhost:8000/health
- **Grafana:** http://localhost:3000
- **Prometheus:** http://localhost:9090
- **Neo4j Browser:** http://localhost:7474

### Проверка через PowerShell

```powershell
# Проверка статуса контейнеров
docker ps

# Проверка логов
docker compose -f E:\TERAG\docker-compose.yml logs

# Проверка API
curl http://localhost:8000/health
```

## Что установлено

После успешной установки на диске **E:\TERAG** будет создана следующая структура:

```
E:\TERAG\
├── app\                    # FastAPI приложение
├── data\                   # Данные (Neo4j, логи, кэш)
├── prometheus\             # Конфигурация Prometheus
├── grafana\                # Конфигурация Grafana
├── docker-compose.yml      # Docker Compose конфигурация
└── config.env              # Переменные окружения
```

## Управление сервисами

### Остановка
```powershell
cd E:\TERAG
docker compose down
```

### Запуск
```powershell
cd E:\TERAG
docker compose up -d
```

### Перезапуск
```powershell
cd E:\TERAG
docker compose restart
```

### Просмотр логов
```powershell
cd E:\TERAG
docker compose logs -f
```

## Устранение проблем

### Проблема: Docker не найден
**Решение:** Установите Docker Desktop с https://www.docker.com/products/docker-desktop

### Проблема: Порты заняты
**Решение:** Измените порты в `config.env`:
```
FASTAPI_PORT=8001
GRAFANA_PORT=3001
PROMETHEUS_PORT=9091
```

### Проблема: Neo4j не запускается
**Решение:** 
1. Проверьте логи: `docker compose logs neo4j`
2. Убедитесь, что порты 7474 и 7687 свободны
3. Проверьте права доступа к `E:\TERAG\data\neo4j`

### Проблема: Недостаточно места на диске
**Решение:** Освободите место на диске E: или измените путь установки

## Следующие шаги

После установки:

1. **Настройте Grafana:**
   - Откройте http://localhost:3000
   - Войдите (admin/terag_admin)
   - Prometheus datasource уже настроен

2. **Подключитесь к Neo4j:**
   - Откройте http://localhost:7474
   - Войдите (neo4j/terag_local)
   - Смените пароль при первом входе

3. **Протестируйте API:**
   - Откройте http://localhost:8000/docs
   - Попробуйте endpoint `/context` с примером запроса

## Дополнительная информация

- Полная документация: `README.md`
- Конфигурация: `config.env`
- Docker Compose: `docker-compose.yml`

---

**Версия:** 1.0.0  
**Дата:** 2025-01-27





















