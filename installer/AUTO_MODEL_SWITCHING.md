# 🤖 Автоматическое переключение моделей в TERAG

## Обзор

TERAG теперь автоматически выбирает оптимальную модель Ollama в зависимости от типа задачи, обеспечивая лучшие результаты для каждого запроса.

## 🎯 Типы задач

### CODE (Код)
- Генерация кода
- Анализ кода
- Отладка
- Рефакторинг
- Программирование

**Приоритет моделей:**
1. `qwen3-coder` / `qwen3coder`
2. `deepseek-coder` / `deepseekcoder`
3. `codellama`
4. `qwen2.5-coder`
5. `llama3`, `phi3`

### ANALYSIS (Анализ)
- Анализ данных
- Исследование
- Извлечение информации
- Сравнение
- Резюме

**Приоритет моделей:**
1. `qwen3` / `qwen2.5`
2. `llama3`
3. `mistral`
4. `phi3`, `gemma`

### REASONING (Рассуждение)
- Сложные задачи
- Решение проблем
- Логический вывод
- Объяснение

**Приоритет моделей:**
1. `qwen3` / `qwen2.5`
2. `llama3`
3. `mistral`
4. `phi3`

### GENERAL (Общее)
- Общие вопросы
- Разговор
- Информационные запросы

**Приоритет моделей:**
1. `llama3`
2. `qwen3` / `qwen2.5`
3. `mistral`
4. `phi3`, `gemma`

## 🚀 Использование

### Автоматическое определение (рекомендуется)

По умолчанию TERAG автоматически определяет тип задачи и выбирает модель:

```powershell
curl -X POST http://localhost:8000/context `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Как исправить ошибку в этом коде?",
    "use_llm": true,
    "auto_select_model": true
  }'
```

Система автоматически:
1. ✅ Определит тип задачи (CODE)
2. ✅ Выберет лучшую модель (`qwen3-coder`)
3. ✅ Выполнит запрос
4. ✅ Вернёт результат с метаданными

### Явное указание типа задачи

Можно явно указать тип задачи:

```powershell
curl -X POST http://localhost:8000/context `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Проанализируй данные",
    "use_llm": true,
    "auto_select_model": true,
    "task_type": "analysis"
  }'
```

### Отключение автоматического выбора

Если нужно использовать текущую модель без переключения:

```powershell
curl -X POST http://localhost:8000/context `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Любой вопрос",
    "use_llm": true,
    "auto_select_model": false
  }'
```

## 🧪 Тестирование выбора модели

Проверить, какая модель будет выбрана для вопроса:

```powershell
curl -X POST http://localhost:8000/llm/test-task-selection `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Напиши функцию для сортировки массива"
  }'
```

Ответ:
```json
{
  "question": "Напиши функцию для сортировки массива",
  "detected_task_type": "code",
  "selected_model": "qwen3-coder:latest",
  "current_model": "llama3:8b",
  "would_switch": true,
  "available_models": ["qwen3-coder:latest", "llama3:8b", ...],
  "task_priorities": {
    "code": ["qwen3-coder", "deepseek-coder", ...],
    ...
  }
}
```

## 📊 Метаданные в ответе

Каждый ответ содержит информацию о выборе модели:

```json
{
  "answer": "...",
  "metadata": {
    "llm_metadata": {
      "model": "qwen3-coder:latest",
      "model_selected": "qwen3-coder:latest",
      "task_type": "code",
      "model_switched": true,
      "provider": "ollama",
      "total_duration": 1234
    }
  }
}
```

## 🔍 Как работает определение задачи

Система анализирует ключевые слова в запросе:

### CODE ключевые слова
- `code`, `function`, `class`, `method`, `variable`, `import`
- `debug`, `error`, `bug`, `fix`, `implement`, `programming`
- `algorithm`, `syntax`, `compile`, `test`, `refactor`
- Языки программирования: `python`, `javascript`, `typescript`, `java`, `c++`, `go`, `rust`

### ANALYSIS ключевые слова
- `analyze`, `analysis`, `extract`, `parse`, `data`
- `research`, `study`, `examine`, `evaluate`, `compare`
- `summary`, `summarize`, `findings`, `trends`

### REASONING ключевые слова
- `why`, `how`, `explain`, `reasoning`, `logic`, `think`
- `solve`, `problem`, `strategy`, `approach`, `decision`
- `conclusion`, `inference`, `deduce`

## 💡 Примеры использования

### Пример 1: Код

```powershell
# Автоматически выберет qwen3-coder
curl -X POST http://localhost:8000/context `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Реализуй функцию для поиска бинарным поиском на Python",
    "use_llm": true
  }'
```

### Пример 2: Анализ

```powershell
# Автоматически выберет qwen3 или llama3
curl -X POST http://localhost:8000/context `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Проанализируй тренды в искусственном интеллекте за 2025 год",
    "use_llm": true
  }'
```

### Пример 3: Рассуждение

```powershell
# Автоматически выберет qwen3 или llama3
curl -X POST http://localhost:8000/context `
  -H "Content-Type: application/json" `
  -d '{
    "question": "Объясни, почему квантовые компьютеры могут решить проблемы, недоступные классическим",
    "use_llm": true
  }'
```

## ⚙️ Настройка приоритетов

Приоритеты моделей можно изменить в `modules/llm_client.py`:

```python
task_priorities = {
    TaskType.CODE: [
        "qwen3-coder",      # Ваша модель с высшим приоритетом
        "deepseek-coder",
        # ...
    ],
    # ...
}
```

## 📈 Преимущества

- ✅ **Автоматическая оптимизация** - лучшая модель для каждой задачи
- ✅ **Прозрачность** - видно, какая модель использовалась
- ✅ **Гибкость** - можно отключить или задать тип задачи явно
- ✅ **Эффективность** - модели переключаются только при необходимости

## 🔗 Связанные документы

- [Auto Model Detection](AUTO_MODEL_DETECTION.md) - автоопределение доступных моделей
- [LLM Integration](LLM_INTEGRATION.md) - интеграция LLM
- [TERAG API Documentation](http://localhost:8000/docs) - интерактивная документация



















