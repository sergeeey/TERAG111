# ⚙️ Автоматическая валидация конфигурации TERAG

## Обзор

TERAG теперь автоматически проверяет и исправляет `config.env` перед запуском контейнеров, предотвращая ошибки из-за отсутствующих переменных окружения.

## 🔧 Что проверяется

### Обязательные переменные

- `DATA_PATH` - путь к данным (автоматически: `E:\TERAG\data`)
- `NEO4J_URI` - URI подключения к Neo4j (должно быть: `bolt://terag-neo4j:7687`)
- `NEO4J_USER` - пользователь Neo4j (по умолчанию: `neo4j`)
- `NEO4J_PASSWORD` - пароль Neo4j (по умолчанию: `terag_local`)
- `PROMETHEUS_PORT` - порт Prometheus (по умолчанию: `9090`)
- `GRAFANA_PORT` - порт Grafana (по умолчанию: `3000`)
- `FASTAPI_PORT` - порт API (по умолчанию: `8000`)

### Опциональные переменные

- `LLM_PROVIDER` - провайдер LLM (по умолчанию: `ollama`)
- `LLM_URL` - URL LLM сервера (по умолчанию: `http://host.docker.internal:11434`)
- `LLM_MODEL` - модель LLM (опционально, автоопределяется)

## 🚀 Автоматическая валидация

### В rebuild_api.ps1

При пересборке API контейнера валидация выполняется автоматически:

```powershell
.\rebuild_api.ps1 -InstallPath "E:\TERAG"
```

Скрипт автоматически:
1. ✅ Проверяет наличие всех обязательных переменных
2. ✅ Добавляет недостающие переменные со значениями по умолчанию
3. ✅ Исправляет некорректные значения (например, `NEO4J_URI`)
4. ✅ Сохраняет обновлённый `config.env`

### В fix_and_restart.ps1

При исправлении и перезапуске контейнеров:

```powershell
.\fix_and_restart.ps1 -InstallPath "E:\TERAG"
```

## 📋 Ручная валидация

Можно запустить валидацию отдельно:

```powershell
# Только проверка (без исправления)
.\validate_config.ps1 -InstallPath "E:\TERAG"

# Проверка и автоматическое исправление
.\validate_config.ps1 -InstallPath "E:\TERAG" -FixMissing
```

### Пример вывода

```
========================================
  TERAG Config Validator
========================================

  ⚠️  Missing variables: NEO4J_URI, LLM_URL
  🔧 Fixing missing variables...
  ✅ Fixed variables: NEO4J_URI, LLM_URL
  ✅ Config saved to: E:\TERAG\config.env

Current configuration:
  DATA_PATH = E:\TERAG\data
  NEO4J_URI = bolt://terag-neo4j:7687
  NEO4J_USER = neo4j
  NEO4J_PASSWORD = [hidden]
  ...
```

## 🔍 Исправление проблем

### Проблема: NEO4J_URI неправильный

**Симптом:** Контейнер не может подключиться к Neo4j

**Решение:** Валидатор автоматически исправляет на `bolt://terag-neo4j:7687`

### Проблема: Отсутствуют обязательные переменные

**Симптом:** Docker Compose выдаёт предупреждения о пустых переменных

**Решение:** Валидатор автоматически добавляет все недостающие переменные

### Проблема: DATA_PATH неправильный

**Симптом:** Контейнеры не могут найти данные

**Решение:** Валидатор автоматически устанавливает правильный путь на основе `InstallPath`

## 📝 Формат config.env

После валидации файл будет иметь структуру:

```env
# TERAG Local Environment Configuration
# Auto-generated/validated configuration file

# Data Path
DATA_PATH=E:\TERAG\data

# Neo4j Configuration
NEO4J_URI=bolt://terag-neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=terag_local

# Prometheus Configuration
PROMETHEUS_PORT=9090

# Grafana Configuration
GRAFANA_PORT=3000

# FastAPI Configuration
FASTAPI_PORT=8000

# LLM Integration (optional)
LLM_PROVIDER=ollama
LLM_URL=http://host.docker.internal:11434
LLM_MODEL=qwen3-coder
```

## 💡 Рекомендации

1. **Всегда используйте `rebuild_api.ps1`** - он автоматически валидирует конфигурацию
2. **Проверяйте конфигурацию перед запуском** - используйте `validate_config.ps1`
3. **Не редактируйте config.env вручную** без необходимости - валидатор может перезаписать изменения
4. **Сохраняйте кастомные значения** - валидатор сохраняет существующие значения, если они корректны

## 🔗 Связанные документы

- [Setup Guide](SETUP_GUIDE.md)
- [LLM Integration](LLM_INTEGRATION.md)
- [Auto Model Detection](AUTO_MODEL_DETECTION.md)



















