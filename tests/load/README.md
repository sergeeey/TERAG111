# Нагрузочное тестирование SSE endpoints

Скрипт для нагрузочного тестирования SSE (Server-Sent Events) endpoints TERAG.

## 📋 Описание

`load_test_sse.py` позволяет:
- Тестировать производительность SSE endpoints под нагрузкой
- Измерять latency, throughput, error rate
- Генерировать отчеты с метриками
- Проверять стабильность при множественных подключениях

## 🚀 Использование

### Базовый запуск

```bash
python tests/load/load_test_sse.py
```

### С параметрами

```bash
python tests/load/load_test_sse.py \
  --endpoint http://localhost:8000/api/stream/reasoning \
  --connections 20 \
  --duration 120 \
  --query "What is Graph-RAG?" \
  --show planner solver \
  --output-dir docs/load_test_results
```

## 📊 Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--endpoint` | URL SSE endpoint | `http://localhost:8000/api/stream/reasoning` |
| `--connections` | Количество параллельных подключений | `10` |
| `--duration` | Длительность теста (секунды) | `60` |
| `--query` | Запрос для тестирования | `"What is Graph-RAG?"` |
| `--show` | Типы узлов для фильтрации | `None` |
| `--output-dir` | Директория для сохранения результатов | `docs/load_test_results` |
| `--timeout` | Таймаут подключения (секунды) | `30` |

## 📈 Метрики

Скрипт собирает следующие метрики:

### Основные метрики
- **Successful connections** — количество успешных подключений
- **Failed connections** — количество неуспешных подключений
- **Total events** — общее количество полученных событий
- **Avg events per connection** — среднее количество событий на подключение

### Latency метрики
- **Average latency** — средняя задержка первого события
- **Median latency** — медианная задержка
- **95th percentile latency** — 95-й перцентиль задержки

### Производительность
- **Error rate** — процент ошибок
- **Throughput** — количество событий в секунду
- **Average duration** — средняя длительность подключения

## 📄 Формат отчета

Результаты сохраняются в JSON файл:

```json
{
  "results": {
    "total_connections": 10,
    "successful_connections": 9,
    "failed_connections": 1,
    "avg_first_event_latency_ms": 45.2,
    "error_rate": 10.0,
    "throughput_events_per_second": 15.3
  },
  "individual_metrics": [
    {
      "connection_id": "0",
      "success": true,
      "events_received": 5,
      "first_event_latency_ms": 42.1
    }
  ]
}
```

## 🔧 Требования

- Python 3.11+
- `aiohttp` — для асинхронных HTTP запросов
- Запущенный TERAG API сервер

### Установка зависимостей

```bash
pip install aiohttp
```

## 📝 Примеры использования

### Тест с 50 параллельными подключениями

```bash
python tests/load/load_test_sse.py --connections 50 --duration 180
```

### Тест с фильтрацией узлов

```bash
python tests/load/load_test_sse.py \
  --query "Explain quantum computing" \
  --show planner solver verifier
```

### Длительный тест стабильности

```bash
python tests/load/load_test_sse.py \
  --connections 10 \
  --duration 600 \
  --timeout 60
```

## ⚠️ Рекомендации

1. **Начните с малого** — сначала протестируйте с небольшим количеством подключений
2. **Мониторинг сервера** — следите за использованием ресурсов сервера во время теста
3. **Постепенное увеличение** — увеличивайте нагрузку постепенно
4. **Анализ результатов** — проверяйте error rate и latency для выявления проблем

## 🎯 Критерии успеха

- ✅ Error rate < 1%
- ✅ Average latency < 100ms
- ✅ 95th percentile latency < 500ms
- ✅ Throughput > 10 events/sec

## 🔗 Связанные документы

- [Phase 5 Review Fix Summary](../../docs/PHASE5_REVIEW_FIX_SUMMARY.md)
- [Testing Guide](../../TESTING_GUIDE.md)
- [SSE Stream API Documentation](../../docs/API.md)







