# ⚙️ Configs Directory

Директория для всех конфигурационных файлов проекта TERAG.

## 📁 Структура

```
configs/
├── cursor/        # Конфигурации Cursor IDE
├── frontend/      # Конфигурации фронтенда
└── backend/       # Конфигурации бэкенда
```

## 🔧 Категории конфигураций

### Cursor (`cursor/`)
Конфигурационные файлы для Cursor IDE:
- `cursor_config.json` - Основная конфигурация Cursor
- `cursor_ollama_config.json` - Конфигурация Ollama интеграции
- `cursor_quick_config.json` - Быстрая конфигурация
- `cursor_task*.json` - Задачи для Cursor

### Frontend (`frontend/`)
Конфигурации фронтенда (React, Vite, TypeScript):
- `vite.config.ts` - Конфигурация Vite
- `tailwind.config.js` - Конфигурация Tailwind CSS
- `tsconfig.json` - Конфигурация TypeScript
- `eslint.config.js` - Конфигурация ESLint

### Backend (`backend/`)
Конфигурации бэкенда (Python):
- `pyproject.toml` - Конфигурация проекта Python
- `requirements.txt` - Зависимости Python (может быть в корне)

## 📝 Использование

Конфигурации автоматически загружаются соответствующими инструментами:

- **Cursor** ищет конфиги в `configs/cursor/`
- **Vite** использует `configs/frontend/vite.config.ts`
- **Python** использует `configs/backend/pyproject.toml`

## ⚠️ Важно

- Не коммитить **секреты** в конфигурационные файлы
- Использовать `.env` для чувствительных данных
- Документировать изменения в конфигурациях
