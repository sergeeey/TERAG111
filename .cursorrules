# Cursor Rules for TERAG Project
# Автоматические правила для предотвращения ошибок

## 📚 Системный контекст

**ВАЖНО:** Перед началом работы с проектом TERAG обязательно изучи:
- `.cursor/terag_context.md` — полный системный контекст TERAG, архитектура, протоколы, шаблоны
- `.cursor/memory_bank/project_overview.md` — обзор проекта и компонентов
- `docs/architecture/kag-integration.md` — архитектура KAG интеграции

Этот контекст определяет, как Cursor должен интерпретировать запросы и генерировать код в рамках Graph-RAG экосистемы TERAG.

## 🚨 Критические правила

### 1. Кириллица = edit_file
- Если файл содержит русский текст → ВСЕГДА использовать `edit_file`
- НЕ использовать `apply_patch` для файлов с кириллицей
- Проверять содержимое перед выбором инструмента

### 2. .env файлы
- НЕ создавать `.env.template` (заблокировано)
- Использовать `.env.example` или `config/env.sample`
- Всегда проверять globalIgnore перед созданием

### 3. Директории
- ВСЕГДА проверять существование директории перед созданием файла
- Использовать `list_dir` перед `write` или `edit_file`
- Создавать директории через `write` с пустым содержимым

### 4. Дублирование
- Проверять существование `src/` и `app/` перед созданием UI
- Выбирать ОДИН фреймворк (Vite или Next.js)
- Удалять неиспользуемые структуры

## 🎯 Алгоритм действий

### Перед созданием файла:
1. `list_dir` для проверки директории
2. Если кириллица → `edit_file`
3. Если .env → альтернативное имя
4. Проверить дублирование

### При ошибке:
1. Записать в `.memory/errors.md`
2. Найти решение в `.memory/project-patterns.md`
3. Применить решение
4. Обновить паттерны

### Перед коммитом:
1. `npm run check-errors`
2. `npm run lint`
3. `npm run test`
4. Обновить CHANGELOG.md

## 🧠 Когнитивные принципы

- **Паттерн-распознавание**: Анализировать каждую ошибку
- **Автоматизация**: Создавать скрипты для рутины
- **Непрерывное обучение**: Обновлять правила при новых ошибках
- **Эмоциональная нейтральность**: Решать проблемы по алгоритму






