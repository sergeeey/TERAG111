# 🚀 TERAG - Создание ярлыка для запуска

## Быстрый способ: Двойной клик по `.bat` файлу

Самый простой способ — использовать готовый batch-файл:

### Просто запустите:
```
start_TERAG.bat
```

Это автоматически:
1. ✅ Запустит API сервер
2. ✅ Откроет дашборд в браузере

---

## Альтернатива: PowerShell скрипт

Для более продвинутого управления используйте:

```
run_TERAG.ps1
```

Запуск из PowerShell:
```powershell
.\run_TERAG.ps1
```

---

## Создание ярлыка на рабочем столе

### Вариант 1: Ярлык для `.bat` файла

1. **Правой кнопкой** на `start_TERAG.bat`
2. **Создать ярлык**
3. Переместите ярлык на рабочий стол
4. Переименуйте в `TERAG AI System`
5. (Опционально) Смените иконку

### Вариант 2: Ярлык для PowerShell скрипта

1. **На рабочем столе**: Правая кнопка → Создать → Ярлык
2. **Введите путь:**
   ```
   powershell.exe -ExecutionPolicy Bypass -File "D:\TERAG111-1\run_TERAG.ps1"
   ```
3. **Название**: TERAG AI System
4. **Готово!**

---

## Добавление красивого значока

### Вариант 1: Использовать эмодзи
В свойствах ярлыка можно использовать эмодзи как иконку:
- 🧠 (мозг)
- 🚀 (ракета)
- ⚡ (молния)
- 🤖 (робот)

### Вариант 2: Загрузить .ico файл
1. Найдите подходящую иконку в формате `.ico`
2. В свойствах ярлыка → Изменить значок
3. Укажите путь к файлу

---

## Автозапуск TERAG при старте Windows

### Способ 1: Через автозагрузку

1. Нажми `Win + R`
2. Введите `shell:startup`
3. Скопируйте ярлык `TERAG AI System` в эту папку

### Способ 2: Через Task Scheduler

Создайте задачу в планировщике:
- Запуск: При входе в Windows
- Действие: Запуск программы
- Программа: `D:\TERAG111-1\start_TERAG.bat`

---

## Устранение проблем

### Проблема: "Выполнение сценариев отключено"

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: Порт 8000 занят

**Решение:**
```powershell
# Найти процесс
netstat -ano | findstr :8000

# Остановить процесс
taskkill /PID <номер_процесса> /F
```

### Проблема: Python не найден

**Решение:**
```powershell
# Добавить Python в PATH
$env:Path += ";C:\Python\Scripts;C:\Python"
```

---

## Полезные команды

### Остановить TERAG
```powershell
# Найти и остановить все Python процессы
Get-Process python | Stop-Process
```

### Проверить статус
```bash
curl http://127.0.0.1:8000/api/health
```

### Проверить метрики
```bash
curl http://127.0.0.1:8000/api/metrics
```

---

## Быстрый доступ

После создания ярлыка, вы можете:

- ✅ **Запускать TERAG двойным кликом**
- ✅ **Видеть статус в отдельном окне**
- ✅ **Автоматический открытие дашборда**
- ✅ **Легкое управление сервером**

---

## Готово! 🎉

Теперь TERAG всегда под рукой — один двойной клик, и система запущена!



























